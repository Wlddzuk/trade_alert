---
phase: 05-monitoring-audit-and-review-surface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/audit/review_models.py
  - backend/app/audit/review_service.py
  - backend/app/audit/pnl_summary.py
  - backend/tests/audit_review/test_trade_review_groups.py
  - backend/tests/audit_review/test_pnl_summary.py
autonomous: true
requirements:
  - OPS-03
  - OPS-04
user_setup: []
must_haves:
  truths:
    - Immutable lifecycle data can be shaped into day-grouped completed-trade review records without relying on mutable broker state.
    - Paper-P&L summaries can be derived as today-first, realized-first read models with cumulative secondary context and simple day-by-day history.
    - Raw lifecycle events remain available as secondary detail behind the human-readable review rather than becoming the default operator presentation.
  artifacts:
    - backend/app/audit/review_models.py
    - backend/app/audit/review_service.py
    - backend/app/audit/pnl_summary.py
    - backend/tests/audit_review/test_trade_review_groups.py
    - backend/tests/audit_review/test_pnl_summary.py
  key_links:
    - Review grouping builds on the existing immutable lifecycle and `TradeReview` seams from Phase 4.
    - P&L summaries derive from closed-trade lifecycle outcomes and stay aligned with review data rather than using a second independent source of truth.
---

<objective>
Implement immutable audit review and paper-P&L summaries for Phase 5.

Purpose: turn the Phase 4 lifecycle stream into operator-facing review and performance read models that the dashboard can present without mutating or reinterpreting trade history.
Output: grouped review models, day-first review summaries, raw-event drill-down hooks, today-first P&L summaries, and day-by-day performance history with automated tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-monitoring-audit-and-review-surface/05-CONTEXT.md
@.planning/phases/05-monitoring-audit-and-review-surface/05-RESEARCH.md
@.planning/phases/05-monitoring-audit-and-review-surface/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement day-grouped trade-review read models with summary-first drill-down</name>
  <files>backend/app/audit/review_models.py, backend/app/audit/review_service.py, backend/tests/audit_review/test_trade_review_groups.py</files>
  <action>Implement the Phase 5 review layer over immutable lifecycle data. It must group completed trades by trading day, order newest trades first within each day, expose summary-first review records for the default operator view, and preserve raw lifecycle events as secondary drill-down detail rather than the primary presentation.</action>
  <verify>cd backend && uv run pytest tests/audit_review/test_trade_review_groups.py -q</verify>
  <done>Completed trades can be grouped and summarized by trading day with human-readable review first and raw events still available as secondary detail.</done>
</task>

<task type="auto">
  <name>Task 2: Implement realized-first paper-P&L summaries and day-by-day history</name>
  <files>backend/app/audit/pnl_summary.py, backend/app/audit/review_service.py, backend/tests/audit_review/test_pnl_summary.py</files>
  <action>Implement the Phase 5 P&L summary layer over immutable lifecycle outcomes. It must emphasize today's realized P&L first, provide cumulative secondary context, include trade count and win rate as companion metrics, and expose simple day-by-day history without introducing chart-first analytics.</action>
  <verify>cd backend && uv run pytest tests/audit_review/test_pnl_summary.py -q</verify>
  <done>Phase 5 can provide today-first, realized-first paper-P&L summaries plus simple historical day-by-day performance derived from lifecycle events.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/audit_review/test_trade_review_groups.py tests/audit_review/test_pnl_summary.py -q
- [ ] Completed trades are grouped by trading day with newest-first ordering inside each day
- [ ] Review data derives from immutable lifecycle inputs rather than mutable trade state
- [ ] Raw lifecycle events remain secondary detail behind the human-readable review
- [ ] P&L summaries are today-first and realized-first with cumulative secondary context
- [ ] Day-by-day history exposes trade count and win rate alongside P&L
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 has review and performance read models ready for dashboard composition
</success_criteria>

<output>
After completion, create `.planning/phases/05-monitoring-audit-and-review-surface/05-02-SUMMARY.md`
</output>

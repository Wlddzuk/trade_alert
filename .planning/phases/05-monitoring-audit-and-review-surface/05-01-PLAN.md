---
phase: 05-monitoring-audit-and-review-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/ops/monitoring_models.py
  - backend/app/ops/overview_service.py
  - backend/app/ops/incident_log.py
  - backend/app/ops/alert_delivery_health.py
  - backend/tests/ops_dashboard/test_status_overview.py
  - backend/tests/ops_dashboard/test_incident_log.py
  - backend/tests/ops_dashboard/test_alert_delivery_health.py
autonomous: true
requirements:
  - OPS-01
  - OPS-02
  - OPS-05
user_setup: []
must_haves:
  truths:
    - The system can expose a status-first operational overview that distinguishes healthy, degraded, recovering, and offline/session-closed states without recomputing trust semantics in the dashboard layer.
    - Provider freshness ages, scanner-loop health, and alert-delivery health are available as explicit read models for Phase 5 overview consumption.
    - Recent critical issues and recently resolved incidents are available as a separate operational history surface instead of being mixed into the landing overview.
  artifacts:
    - backend/app/ops/monitoring_models.py
    - backend/app/ops/overview_service.py
    - backend/app/ops/incident_log.py
    - backend/app/ops/alert_delivery_health.py
    - backend/tests/ops_dashboard/test_status_overview.py
    - backend/tests/ops_dashboard/test_incident_log.py
    - backend/tests/ops_dashboard/test_alert_delivery_health.py
  key_links:
    - Overview read models consume existing `SystemTrustSnapshot` and `SystemEvent` outputs rather than inferring trust from raw provider timestamps.
    - Alert-delivery health is modeled alongside provider freshness and scanner-loop health so operator trust can account for transport failures too.
    - Incident history stays separate from current overview state so the Phase 5 dashboard can remain summary-first.
---

<objective>
Implement degraded-state logic and operational monitoring surfaces for Phase 5.

Purpose: shape the current trust, health, and incident state the operator needs to see before any dashboard rendering is added.
Output: explicit overview and incident read models for trust state, freshness, scanner-loop health, alert-delivery health, recent critical issues, and recent resolved issues, plus automated tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-monitoring-audit-and-review-surface/05-CONTEXT.md
@.planning/phases/05-monitoring-audit-and-review-surface/05-RESEARCH.md
@.planning/phases/05-monitoring-audit-and-review-surface/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement current-status overview read models for trust, freshness, loop health, and delivery health</name>
  <files>backend/app/ops/monitoring_models.py, backend/app/ops/overview_service.py, backend/tests/ops_dashboard/test_status_overview.py</files>
  <action>Implement the Phase 5 operational overview read layer. It must expose summary-first models for current trust state, provider freshness ages, scanner-loop health, alert-delivery health, and neutral offline/session-closed status using the existing runtime and trust contracts from earlier phases. Keep this backend-focused and do not build UI rendering in this plan.</action>
  <verify>cd backend && uv run pytest tests/ops_dashboard/test_status_overview.py -q</verify>
  <done>The system can produce a status-first current overview with healthy/degraded/recovering/offline states plus the prioritized operational summary fields the dashboard will need.</done>
</task>

<task type="auto">
  <name>Task 2: Implement recent incident history and recent alert-failure surfaces</name>
  <files>backend/app/ops/incident_log.py, backend/app/ops/alert_delivery_health.py, backend/app/ops/overview_service.py, backend/tests/ops_dashboard/test_incident_log.py, backend/tests/ops_dashboard/test_alert_delivery_health.py</files>
  <action>Implement the Phase 5 operational-history surface over recent critical issues, recent resolved incidents, and recent alert-delivery failures. It must preserve the distinction between current overview state and recent history so the landing dashboard stays concise while still giving the operator confidence about what just failed or recovered.</action>
  <verify>cd backend && uv run pytest tests/ops_dashboard/test_incident_log.py tests/ops_dashboard/test_alert_delivery_health.py -q</verify>
  <done>Phase 5 has explicit recent-incident and alert-failure read models that can power a summary-first dashboard and a deeper read-only logs section without dumping raw logs on the landing screen.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/ops_dashboard/test_status_overview.py tests/ops_dashboard/test_incident_log.py tests/ops_dashboard/test_alert_delivery_health.py -q
- [ ] Current status distinguishes healthy, degraded, recovering, and offline/session-closed states
- [ ] Provider freshness, scanner-loop health, and alert-delivery health are summarized explicitly
- [ ] Recent critical issues remain separate from current overview state
- [ ] Recently resolved incidents remain available after recovery
- [ ] No dashboard rendering or action controls are introduced in this plan
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 5 has an operator-usable ops read layer ready for dashboard composition
</success_criteria>

<output>
After completion, create `.planning/phases/05-monitoring-audit-and-review-surface/05-01-SUMMARY.md`
</output>

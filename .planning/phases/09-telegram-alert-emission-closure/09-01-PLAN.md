---
phase: 09-telegram-alert-emission-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/alerts/alert_emission.py
  - backend/app/alerts/telegram_runtime.py
  - backend/app/alerts/action_resolution.py
  - backend/app/audit/lifecycle_log.py
  - backend/app/scanner/feed_service.py
  - backend/app/main.py
  - backend/tests/operator_workflow/test_telegram_alert_emission_flow.py
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
user_setup: []
must_haves:
  truths:
    - Operators can receive a qualifying Telegram alert through one shipped runtime path that starts from qualifying setup input and ends at successful message delivery.
    - Alerts that are suppressed as duplicates or fail delivery do not appear as new operator-actionable emitted alerts.
    - A successfully delivered qualifying alert remains available for the operator’s later approval or adjustment actions through the Telegram workflow.
    - Successfully delivered qualifying alerts are recorded as matching pre-entry alert events for later operator review.
  artifacts:
    - backend/app/alerts/alert_emission.py
    - backend/app/alerts/telegram_runtime.py
    - backend/app/alerts/action_resolution.py
    - backend/app/audit/lifecycle_log.py
    - backend/app/scanner/feed_service.py
    - backend/app/main.py
    - backend/tests/operator_workflow/test_telegram_alert_emission_flow.py
  key_links:
    - The emission orchestrator must reuse `render_pre_entry_alert(...)`, `TelegramRuntimeDeliveryService.deliver(...)`, and `TelegramActionRegistry.register_alert(...)` in one shipped loop.
    - `TelegramDeliveryState` remains the authority for dedupe and state-transition emission policy rather than being reimplemented inside the new service.
    - A real qualifying-setup producer seam such as scanner/runtime feed handling must invoke the emission path so emitted alerts originate from shipped runtime code rather than manual helper invocation.
---

<objective>
Implement the qualifying-alert emission path that closes the remaining Telegram producer gap for Phase 09.

Purpose: connect qualifying setup production to rendered Telegram delivery, registry registration, and emitted-alert audit evidence in one production-facing runtime flow.
Output: an emission application service, qualifying-setup producer wiring, narrow runtime composition for that path, and producer-path tests that prove successful, suppressed, duplicate, and failed-send behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-telegram-alert-emission-closure/09-RESEARCH.md
@.planning/phases/09-telegram-alert-emission-closure/09-VALIDATION.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the qualifying-alert emission orchestrator over existing projection, delivery, and registry seams</name>
  <files>backend/app/alerts/alert_emission.py, backend/app/alerts/telegram_runtime.py, backend/app/alerts/action_resolution.py, backend/tests/operator_workflow/test_telegram_alert_emission_flow.py</files>
  <action>Create one runtime-facing emission service that accepts the qualifying setup contract already recommended in `09-RESEARCH.md`, builds the `PreEntryAlert`, consults `TelegramDeliveryState`, renders the alert, sends it through `TelegramRuntimeDeliveryService`, and registers the alert in `TelegramActionRegistry` only after successful delivery. Keep the service orchestration-focused: it should return structured emission outcomes for send, suppression, duplicate, and failure cases without absorbing callback-execution logic or introducing a second state store.</action>
  <verify>cd backend && uv run pytest tests/operator_workflow/test_telegram_alert_emission_flow.py -q</verify>
  <done>The codebase has one production emission path from qualifying setup to outbound Telegram delivery and callback registration, and tests prove successful send, duplicate suppression, and failed-send no-registration behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the emitted-alert service into a real qualifying-setup producer path and record emitted-alert evidence</name>
  <files>backend/app/alerts/alert_emission.py, backend/app/audit/lifecycle_log.py, backend/app/scanner/feed_service.py, backend/app/main.py, backend/tests/operator_workflow/test_telegram_alert_emission_flow.py</files>
  <action>Connect the emission service to a real runtime producer seam that handles qualifying setup output so alerts can leave shipped scanner/runtime code rather than only helper invocation. Reuse the existing qualifying setup models and keep the integration narrow: record pre-entry alert lifecycle evidence only when an alert is actually emitted, provide the minimum runtime wiring or dependency injection needed for operator chat targeting, and avoid expanding into scanner scheduling, queueing, persistence, or broader app redesign.</action>
  <verify>cd backend && uv run pytest tests/operator_workflow/test_telegram_alert_emission_flow.py tests/operator_workflow/test_telegram_runtime_delivery.py -q</verify>
  <done>Qualifying setups can leave real shipped runtime code through the emission path with delivery-coupled lifecycle evidence, and no suppressed or failed path creates false callback continuity.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/operator_workflow/test_telegram_alert_emission_flow.py tests/operator_workflow/test_telegram_runtime_delivery.py -q
- [ ] One production path covers qualifying setup producer -> rendered alert -> send -> registry registration
- [ ] `TelegramDeliveryState` still owns emission dedupe and transition policy
- [ ] Failed or suppressed sends do not register alerts or record false emitted-alert evidence
- [ ] Emission is reachable from shipped qualifying-setup runtime code rather than only from manual helper invocation
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 09 has a shipped emitted-alert path that starts from qualifying setup production and establishes the correct starting state for `FLOW-01`, `FLOW-02`, and `FLOW-03`
</success_criteria>

<output>
After completion, create `.planning/phases/09-telegram-alert-emission-closure/09-01-SUMMARY.md`
</output>

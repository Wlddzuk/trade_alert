---
phase: 09-telegram-alert-emission-closure
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - backend/tests/operator_workflow/test_telegram_callback_routes.py
  - backend/tests/operator_workflow/test_telegram_webhook_serving.py
  - backend/tests/operator_workflow/test_adjustment_sessions.py
  - backend/tests/operator_workflow/test_telegram_alert_emission_webhook_flow.py
autonomous: true
requirements:
  - FLOW-02
  - FLOW-03
  - FLOW-01
user_setup: []
must_haves:
  truths:
    - Operators can approve or reject a real emitted qualifying alert through the callback flow, with approval opening the intended paper trade and rejection completing through the same emitted-alert state.
    - Operators can adjust stop or target on a real emitted qualifying alert and complete adjusted approval through the served webhook boundary.
    - The same real emitted qualifying alert can be handled through both callback-route and served-webhook decision paths.
    - Alerts that were never emitted because of suppression or failed delivery cannot be approved or adjusted through the callback path.
  artifacts:
    - backend/tests/operator_workflow/test_telegram_callback_routes.py
    - backend/tests/operator_workflow/test_telegram_webhook_serving.py
    - backend/tests/operator_workflow/test_adjustment_sessions.py
    - backend/tests/operator_workflow/test_telegram_alert_emission_webhook_flow.py
  key_links:
    - Approval proof must emit first, then approve through `TelegramCallbackHandler` or `TelegramRoutes`, and confirm the opened trade references the emitted alert id.
    - Adjustment proof must emit first, then run the guided adjustment flow and final confirmation from emitted alert state rather than pre-seeded registry state.
    - ASGI-boundary proof must begin with producer emission and complete through `create_app()` without test-only callback pre-registration.
---

<objective>
Replace the remaining partial Telegram evidence with emission-driven approval and adjustment continuity proof for Phase 09.

Purpose: demonstrate that the live callback and served webhook paths operate correctly when their alert state comes from the shipped emitted-alert path rather than from manual test setup.
Output: route-level and ASGI-boundary tests for approve, reject, and adjusted approve flows, plus negative coverage showing that non-emitted alerts are not callback-resolvable.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-telegram-alert-emission-closure/09-RESEARCH.md
@.planning/phases/09-telegram-alert-emission-closure/09-VALIDATION.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/09-telegram-alert-emission-closure/09-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert callback-route approval evidence to start from emitted alert state</name>
  <files>backend/tests/operator_workflow/test_telegram_callback_routes.py, backend/tests/operator_workflow/test_telegram_alert_emission_webhook_flow.py</files>
  <action>Replace or supplement the partial callback-route evidence so approval and rejection tests begin by emitting a qualifying alert through the real emission service from Plan 01. Then drive both approve and reject paths through the live callback handler or route surface and assert that the resulting decision behavior references the emitted alert id and registry state. Keep duplicate and stale-path assertions where they still matter, but stop relying on direct `registry.register_alert(...)` as the primary proof for milestone closure.</action>
  <verify>cd backend && uv run pytest tests/operator_workflow/test_telegram_callback_routes.py tests/operator_workflow/test_telegram_alert_emission_webhook_flow.py -q</verify>
  <done>Approve and reject continuity are proven from emitted qualifying alert through callback handling to operator decision outcomes, and milestone evidence no longer depends on manually seeded alert registry state.</done>
</task>

<task type="auto">
  <name>Task 2: Prove adjusted approval continuity through the served webhook boundary from emitted alert state</name>
  <files>backend/tests/operator_workflow/test_adjustment_sessions.py, backend/tests/operator_workflow/test_telegram_webhook_serving.py, backend/tests/operator_workflow/test_telegram_alert_emission_webhook_flow.py</files>
  <action>Add the end-to-end adjusted approval proof required by `09-RESEARCH.md`: emit the qualifying alert through the producer path, drive `entry:ad:*` and follow-up reply-message confirmation through the served `create_app()` webhook boundary, and assert that the adjusted approval produces the expected entry-decision and trade-opened outcomes from the same emitted alert state. Include at least one negative assertion that failed or suppressed emission paths do not produce callback-resolvable approval or adjustment continuity.</action>
  <verify>cd backend && uv run pytest tests/operator_workflow/test_adjustment_sessions.py tests/operator_workflow/test_telegram_webhook_serving.py tests/operator_workflow/test_telegram_alert_emission_webhook_flow.py -q</verify>
  <done>Adjusted approval continuity is proven through the served webhook boundary from emitted alert state, and failed or suppressed emissions remain non-resolvable through callbacks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/operator_workflow/test_telegram_callback_routes.py tests/operator_workflow/test_adjustment_sessions.py tests/operator_workflow/test_telegram_webhook_serving.py tests/operator_workflow/test_telegram_alert_emission_webhook_flow.py -q
- [ ] At least one approval path starts with real emission and ends with callback-driven trade opening
- [ ] At least one rejection path starts with real emission and ends with callback-driven reject handling
- [ ] At least one adjusted-approval path starts with real emission and completes through served webhook handling
- [ ] Primary milestone evidence no longer relies on direct `registry.register_alert(...)` setup
- [ ] Failed or suppressed emissions are not callback-resolvable
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 09 has end-to-end continuity evidence for approve, reject, and adjusted-approval outcomes grounded in the same emitted-alert path that closes `FLOW-01`, `FLOW-02`, and `FLOW-03`
</success_criteria>

<output>
After completion, create `.planning/phases/09-telegram-alert-emission-closure/09-02-SUMMARY.md`
</output>

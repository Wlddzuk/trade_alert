---
phase: 06-telegram-runtime-delivery-and-callback-wiring
plan: 04
type: execute
gap_closure: true
wave: 3
depends_on:
  - "02"
  - "03"
files_modified:
  - backend/app/main.py
  - backend/app/api/telegram_routes.py
  - backend/app/api/telegram_callbacks.py
  - backend/tests/operator_workflow/test_telegram_callback_routes.py
  - backend/tests/operator_workflow/test_telegram_webhook_serving.py
autonomous: true
requirements:
  - FLOW-02
user_setup: []
must_haves:
  truths:
    - The Telegram callback surface is exposed through a genuinely served HTTP/ASGI application boundary rather than only through in-process helper objects.
    - Telegram updates can reach the existing callback and message handlers through the served boundary without pulling Phase 7 dashboard serving into this gap closure.
    - Automated tests prove callback processing through a live app client or equivalent served request path, not only by calling handler methods directly.
    - The served Telegram boundary preserves the existing stale-safe, duplicate-safe, and operator-readable runtime behavior already implemented in Phase 6.
  artifacts:
    - backend/app/main.py
    - backend/app/api/telegram_routes.py
    - backend/app/api/telegram_callbacks.py
    - backend/tests/operator_workflow/test_telegram_callback_routes.py
    - backend/tests/operator_workflow/test_telegram_webhook_serving.py
  key_links:
    - The gap comes directly from `06-VERIFICATION.md`, which flagged `BuySignalApp.telegram.handle_update(...)` as insufficient proof of a real webhook/runtime endpoint.
    - The served boundary should stay limited to Telegram webhook traffic so Phase 6 does not absorb Phase 7 dashboard runtime scope.
    - Existing callback parsing and execution code should remain the core behavior engine, with the served app layer acting as a thin externally reachable wrapper.
---

<objective>
Close the remaining FLOW-02 verification gap by exposing Telegram callback handling through a genuinely served application boundary.

Purpose: prove that Telegram updates can reach the runtime through a real HTTP/ASGI endpoint instead of only through an in-process route facade.
Output: a minimal served Telegram webhook endpoint, thin app wiring that preserves current behavior, and automated tests that exercise the served boundary.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-CONTEXT.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-RESEARCH.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-VERIFICATION.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a genuinely served Telegram webhook endpoint at the app boundary</name>
  <files>backend/app/main.py, backend/app/api/telegram_routes.py, backend/app/api/telegram_callbacks.py, backend/tests/operator_workflow/test_telegram_webhook_serving.py</files>
  <action>Replace the remaining in-process-only Telegram surface with a minimal served HTTP or ASGI boundary that can accept Telegram webhook updates from outside the process. Keep the route thin and Phase-6-scoped: it should forward updates into the existing callback/message handler logic without dragging in broader dashboard runtime work. The resulting app object should be compatible with the repo's current backend serving approach and expose a concrete endpoint path that later deployment wiring can target.</action>
  <verify>cd backend && uv run pytest tests/operator_workflow/test_telegram_webhook_serving.py -q</verify>
  <done>The app exposes a real served Telegram webhook endpoint, and tests prove updates can be posted through a served request path rather than only by direct method calls.</done>
</task>

<task type="auto">
  <name>Task 2: Route served Telegram requests through the existing callback runtime and preserve stale/idempotent behavior</name>
  <files>backend/app/main.py, backend/app/api/telegram_routes.py, backend/app/api/telegram_callbacks.py, backend/tests/operator_workflow/test_telegram_callback_routes.py, backend/tests/operator_workflow/test_telegram_webhook_serving.py</files>
  <action>Wire the served endpoint into the existing Telegram callback and message execution seams so approve/reject and guided-adjustment follow-up behavior still works through the externally reachable boundary. Prove that stale actions, duplicate callbacks, and operator-readable responses still behave correctly when exercised through the served app layer. Keep the route wrapper thin and avoid re-implementing callback parsing or execution logic inside the HTTP boundary.</action>
  <verify>cd backend && uv run pytest tests/operator_workflow/test_telegram_callback_routes.py tests/operator_workflow/test_telegram_webhook_serving.py -q</verify>
  <done>Telegram updates reach the existing runtime execution path through the served endpoint, and automated tests prove stale, duplicate, and valid actions remain correct through that live boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/operator_workflow/test_telegram_callback_routes.py tests/operator_workflow/test_telegram_webhook_serving.py -q
- [ ] The Telegram runtime surface is externally reachable through a real served endpoint
- [ ] The served endpoint stays narrow and does not pull in broader dashboard runtime scope
- [ ] Valid callbacks still reach the existing execution seams through the served boundary
- [ ] Stale and duplicate callbacks remain operator-readable and safe when exercised through the served path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 6 closes the remaining served-boundary verification gap for FLOW-02
</success_criteria>

<output>
After completion, create `.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-04-SUMMARY.md`
</output>

---
phase: 06-telegram-runtime-delivery-and-callback-wiring
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - backend/app/api/telegram_adjustments.py
  - backend/app/alerts/adjustment_sessions.py
  - backend/app/alerts/telegram_renderer.py
  - backend/app/alerts/approval_workflow.py
  - backend/tests/operator_workflow/test_adjustment_sessions.py
  - backend/tests/operator_workflow/test_telegram_callback_routes.py
autonomous: true
requirements:
  - FLOW-03
  - FLOW-02
user_setup: []
must_haves:
  truths:
    - Alert adjustments use a guided Telegram chat flow rather than a general command bot or one-shot freeform parser.
    - The guided flow allows one-sided stop or target edits while carrying the unchanged proposed value forward.
    - Adjustment sessions support explicit cancel, bounded timeout, and final confirmation before an adjusted approval is opened.
    - Adjustment failures and business-state refusals remain specific and operator-readable, consistent with the chosen stale/duplicate callback posture.
  artifacts:
    - backend/app/api/telegram_adjustments.py
    - backend/app/alerts/adjustment_sessions.py
    - backend/app/alerts/telegram_renderer.py
    - backend/app/alerts/approval_workflow.py
    - backend/tests/operator_workflow/test_adjustment_sessions.py
    - backend/tests/operator_workflow/test_telegram_callback_routes.py
  key_links:
    - Guided adjustment sessions build on the live callback surface from Plan 06-02 rather than bypassing it.
    - Final confirmation should still produce the existing adjusted approval decision model from `backend/app/alerts/approval_workflow.py`.
    - Session expiry and cancel behavior must preserve the strict-but-not-silent Telegram posture chosen in `06-CONTEXT.md`.
---

<objective>
Implement guided Telegram adjustment sessions and confirmation flow for Phase 6.

Purpose: complete the live operator workflow by turning the existing pre-entry adjustment concept into a bounded conversational runtime path with cancel, timeout, and confirmation handling.
Output: adjustment-session state handling, guided Telegram prompts and confirmation messages, and automated tests for the chosen adjustment behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-CONTEXT.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-RESEARCH.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add bounded guided adjustment-session state for stop and target edits</name>
  <files>backend/app/alerts/adjustment_sessions.py, backend/app/api/telegram_adjustments.py, backend/tests/operator_workflow/test_adjustment_sessions.py</files>
  <action>Implement the bounded conversational state machine for actionable-alert adjustments. The session flow must remain narrow and phase-specific: after `Adjust`, it should guide the operator through revising stop and/or target, allow one-sided edits, support explicit cancel, and expire unfinished sessions after a bounded timeout. Keep the state model separate from raw route handling so guided adjustments do not become ad hoc conditional logic scattered across the callback surface.</action>
  <verify>cd backend && uv run pytest tests/operator_workflow/test_adjustment_sessions.py -q</verify>
  <done>Phase 6 has a dedicated guided adjustment-session model with one-sided edits, cancel, and timeout behavior, and tests prove session transitions remain bounded and deterministic.</done>
</task>

<task type="auto">
  <name>Task 2: Add final confirmation and operator-readable failure responses for adjusted approvals</name>
  <files>backend/app/api/telegram_adjustments.py, backend/app/alerts/telegram_renderer.py, backend/app/alerts/approval_workflow.py, backend/tests/operator_workflow/test_adjustment_sessions.py, backend/tests/operator_workflow/test_telegram_callback_routes.py</files>
  <action>Complete the guided adjustment path by rendering confirmation prompts, translating confirmed values into the existing adjusted-approval primitive, and surfacing clear operator-readable responses when an adjustment is refused, stale, expired, or no longer valid. Final confirmation must occur before the adjusted approval opens a paper trade, and error responses must stay specific enough to reflect business-state reasons such as superseded alert, cooldown posture, or expired session.</action>
  <verify>cd backend && uv run pytest tests/operator_workflow/test_adjustment_sessions.py tests/operator_workflow/test_telegram_callback_routes.py -q</verify>
  <done>Adjusted approvals require a final confirmation, operator-readable failures are specific and safe, and tests prove guided adjustments produce the existing adjusted approval model only when the session remains valid.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/operator_workflow/test_adjustment_sessions.py tests/operator_workflow/test_telegram_callback_routes.py -q
- [ ] Guided adjustments remain conversational and bounded rather than becoming a general Telegram command framework
- [ ] Operators can edit only stop or only target while preserving the other proposed value
- [ ] Adjustment sessions support explicit cancel and timeout expiry
- [ ] Final confirmation is required before an adjusted approval is opened
- [ ] Expired, stale, and refused adjustments return specific operator-readable feedback
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 6 completes the live guided adjustment path needed for FLOW-03 without expanding scope
</success_criteria>

<output>
After completion, create `.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-03-SUMMARY.md`
</output>

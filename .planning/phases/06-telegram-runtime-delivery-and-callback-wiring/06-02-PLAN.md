---
phase: 06-telegram-runtime-delivery-and-callback-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/main.py
  - backend/app/api/__init__.py
  - backend/app/api/telegram_routes.py
  - backend/app/api/telegram_callbacks.py
  - backend/app/alerts/action_resolution.py
  - backend/app/alerts/action_execution.py
  - backend/app/paper/broker.py
  - backend/tests/operator_workflow/test_telegram_callback_routes.py
autonomous: true
requirements:
  - FLOW-02
  - FLOW-05
user_setup: []
must_haves:
  truths:
    - The application exposes a minimal live API surface that can receive Telegram webhook or callback updates without turning Phase 6 into a dashboard-serving phase.
    - Callback payloads are parsed into the existing entry-decision and open-trade-command primitives and then dispatched through the existing workflow or broker sinks instead of embedding broker logic in route code.
    - Only the newest actionable alert or current open trade accepts actions; stale and superseded actions are rejected with the chosen operator-readable feedback.
    - Repeated actions are handled idempotently and return current-state feedback rather than silently mutating state twice.
  artifacts:
    - backend/app/main.py
    - backend/app/api/telegram_routes.py
    - backend/app/api/telegram_callbacks.py
    - backend/app/alerts/action_resolution.py
    - backend/app/alerts/action_execution.py
    - backend/app/paper/broker.py
    - backend/tests/operator_workflow/test_telegram_callback_routes.py
  key_links:
    - Callback routing must decode the compact callback payloads already emitted by `backend/app/alerts/telegram_renderer.py`.
    - Action resolution must consult current alert/trade state rather than relying on historical chat state.
    - Route code should translate transport updates into existing approval and open-trade command seams, then hand execution to a dedicated runtime dispatcher that reaches the paper-broker workflow end to end.
---

<objective>
Implement the live Telegram callback route and action-dispatch path for Phase 6.

Purpose: turn Telegram callback traffic into validated operator decisions and override commands while enforcing newest-state-only, stale-action, and idempotent-repeat behavior.
Output: a minimal Telegram webhook/callback API surface, callback parsing and resolution logic, and automated route-level tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-CONTEXT.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-RESEARCH.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a minimal Telegram webhook or callback API surface to the app boundary</name>
  <files>backend/app/main.py, backend/app/api/__init__.py, backend/app/api/telegram_routes.py, backend/tests/operator_workflow/test_telegram_callback_routes.py</files>
  <action>Introduce the smallest live application surface needed for Telegram runtime control. The app must expose a route or routes that can receive Telegram update payloads and pass them into a transport-agnostic callback dispatcher. Keep the new boundary narrow: it exists to support Telegram delivery and callbacks, not to pull the broader Phase 7 dashboard-serving work into this phase.</action>
  <verify>cd backend && uv run pytest tests/operator_workflow/test_telegram_callback_routes.py -q</verify>
  <done>The application exposes a minimal Telegram runtime API surface, tests can send update payloads through it, and the new boundary stays focused on Telegram control traffic rather than general dashboard serving.</done>
</task>

<task type="auto">
  <name>Task 2: Parse callback payloads, resolve current actionability, and emit idempotent stale-safe operator responses</name>
  <files>backend/app/api/telegram_callbacks.py, backend/app/alerts/action_resolution.py, backend/app/alerts/action_execution.py, backend/app/paper/broker.py, backend/tests/operator_workflow/test_telegram_callback_routes.py</files>
  <action>Implement the callback-dispatch path over the new API surface. It must decode existing compact callback payloads for approve, reject, adjust, close, adjust-stop, and adjust-target actions; resolve current alert or trade state by ID; translate valid actions into the existing approval-workflow primitives; and dispatch those approved actions into the current workflow or paper-broker sinks end to end. It must also enforce newest-state-only semantics, reject stale or superseded actions with current-state feedback, and acknowledge duplicate already-applied actions idempotently.</action>
  <verify>cd backend && uv run pytest tests/operator_workflow/test_telegram_callback_routes.py -q</verify>
  <done>Telegram callbacks are turned into normalized entry or trade commands through a live route path, valid actions reach the workflow or paper-broker sinks end to end, stale and superseded actions fail clearly with current-state feedback, and repeated actions are acknowledged without double-applying state changes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/operator_workflow/test_telegram_callback_routes.py -q
- [ ] The app exposes a minimal Telegram runtime route surface
- [ ] Callback payloads map into existing approval and trade-command primitives and reach workflow execution sinks end to end
- [ ] Only newest actionable/current trade states accept actions
- [ ] Stale or superseded actions return operator-readable current-state feedback
- [ ] Duplicate actions are handled idempotently
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 6 has a live callback-routing and execution path that closes the runtime gap for FLOW-02 and FLOW-05
</success_criteria>

<output>
After completion, create `.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-02-SUMMARY.md`
</output>

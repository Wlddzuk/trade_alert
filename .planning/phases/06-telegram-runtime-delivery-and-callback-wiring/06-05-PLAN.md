---
phase: 06-telegram-runtime-delivery-and-callback-wiring
plan: 05
type: execute
gap_closure: true
wave: 4
depends_on:
  - "04"
files_modified:
  - backend/app/alerts/action_execution.py
  - backend/app/alerts/action_resolution.py
  - backend/app/api/telegram_callbacks.py
  - backend/app/paper/broker.py
  - backend/tests/operator_workflow/test_telegram_callback_routes.py
  - backend/tests/operator_workflow/test_open_trade_overrides.py
autonomous: true
requirements:
  - FLOW-05
user_setup: []
must_haves:
  truths:
    - Open-trade `Adjust Stop` and `Adjust Target` Telegram actions execute end to end through the runtime callback path rather than falling back to generic follow-up-needed responses.
    - Stop and target override commands are translated into the existing `adjust_trade_stop` and `adjust_trade_target` workflow primitives before being applied through the paper-broker seam.
    - Operator responses remain specific and current-state-aware for successful overrides, stale actions, duplicate callbacks, and invalid trade states.
    - Automated tests prove stop/target override execution through the live callback surface created by the gap-closure plan for FLOW-02.
  artifacts:
    - backend/app/alerts/action_execution.py
    - backend/app/alerts/action_resolution.py
    - backend/app/api/telegram_callbacks.py
    - backend/app/paper/broker.py
    - backend/tests/operator_workflow/test_telegram_callback_routes.py
    - backend/tests/operator_workflow/test_open_trade_overrides.py
  key_links:
    - `06-VERIFICATION.md` identified that `ADJUST_STOP` and `ADJUST_TARGET` are parsed but never dispatched into runtime broker commands.
    - Existing approval-workflow primitives already include `adjust_trade_stop` and `adjust_trade_target`; this plan should reuse them instead of inventing a parallel override contract.
    - The served endpoint from plan `06-04` should be the verification surface for the final end-to-end override tests.
---

<objective>
Close the remaining FLOW-05 verification gap by wiring open-trade stop and target overrides through the live Telegram runtime.

Purpose: ensure `Adjust Stop` and `Adjust Target` buttons actually execute broker-backed override commands end to end and are verifiable through the served callback boundary.
Output: runtime stop/target override execution, specific operator responses for trade override actions, and automated end-to-end override tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-CONTEXT.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-RESEARCH.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-VERIFICATION.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch open-trade stop and target override callbacks into runtime broker commands</name>
  <files>backend/app/alerts/action_execution.py, backend/app/alerts/action_resolution.py, backend/app/paper/broker.py, backend/tests/operator_workflow/test_open_trade_overrides.py</files>
  <action>Extend the current Telegram action executor so `trade:st:*` and `trade:tg:*` callbacks no longer stop at parsing. The runtime should resolve the targeted open trade, build the existing stop/target adjustment command primitives, apply them through the current broker seam, and update runtime state so current-state feedback reflects the new stop or target immediately. Keep this override path narrow and consistent with the existing close-action execution model.</action>
  <verify>cd backend && uv run pytest tests/operator_workflow/test_open_trade_overrides.py -q</verify>
  <done>Open-trade stop and target override callbacks execute through the broker seam, update trade state, and are covered by focused override tests.</done>
</task>

<task type="auto">
  <name>Task 2: Verify operator-readable stop/target override behavior through the live Telegram callback surface</name>
  <files>backend/app/api/telegram_callbacks.py, backend/app/alerts/action_execution.py, backend/tests/operator_workflow/test_telegram_callback_routes.py, backend/tests/operator_workflow/test_open_trade_overrides.py</files>
  <action>Exercise the new trade override execution path through the live Telegram callback surface, including successful stop and target changes plus stale, duplicate, and invalid-state responses. The operator-facing responses should stay specific and current-state-aware, matching the Phase 6 context decision to be strict but not silent. Reuse the served webhook boundary added by the preceding gap-closure plan so the verification covers the actual runtime path, not just direct method calls.</action>
  <verify>cd backend && uv run pytest tests/operator_workflow/test_telegram_callback_routes.py tests/operator_workflow/test_open_trade_overrides.py -q</verify>
  <done>Stop and target overrides are verified end to end through the live Telegram callback path, and failure responses remain specific, safe, and operator-readable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/operator_workflow/test_telegram_callback_routes.py tests/operator_workflow/test_open_trade_overrides.py -q
- [ ] `Adjust Stop` and `Adjust Target` callbacks execute end to end rather than falling through to generic needs-input responses
- [ ] Override execution reuses existing trade-command workflow primitives and broker seams
- [ ] Successful overrides update current-state feedback immediately
- [ ] Stale, duplicate, and invalid override attempts return specific operator-readable responses through the live callback path
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 6 closes the remaining FLOW-05 runtime override gap
</success_criteria>

<output>
After completion, create `.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-05-SUMMARY.md`
</output>

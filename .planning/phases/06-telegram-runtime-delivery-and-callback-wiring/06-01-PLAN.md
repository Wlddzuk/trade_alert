---
phase: 06-telegram-runtime-delivery-and-callback-wiring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/alerts/telegram_runtime.py
  - backend/app/alerts/telegram_transport.py
  - backend/app/ops/alert_delivery_health.py
  - backend/app/ops/incident_log.py
  - backend/tests/operator_workflow/test_telegram_runtime_delivery.py
  - backend/tests/ops_dashboard/test_telegram_runtime_failures.py
autonomous: true
requirements:
  - FLOW-01
user_setup: []
must_haves:
  truths:
    - Qualifying setup alerts can leave the pure rendering layer through a production-facing Telegram delivery adapter rather than stopping at `RenderedTelegramMessage`.
    - Outbound Telegram sends retry only for a short bounded window before being recorded as failures.
    - Delivery attempts update the existing alert-delivery health and incident-reporting seams so Phase 5 ops surfaces reflect live runtime state.
    - Transport concerns remain separated from alert projection, operator-decision semantics, and paper-broker logic.
  artifacts:
    - backend/app/alerts/telegram_runtime.py
    - backend/app/alerts/telegram_transport.py
    - backend/app/ops/alert_delivery_health.py
    - backend/app/ops/incident_log.py
    - backend/tests/operator_workflow/test_telegram_runtime_delivery.py
    - backend/tests/ops_dashboard/test_telegram_runtime_failures.py
  key_links:
    - Runtime delivery consumes the existing `RenderedTelegramMessage` contract from `backend/app/alerts/telegram_renderer.py`.
    - Delivery attempt recording feeds the existing ops-reporting models instead of inventing a separate health path.
    - Retry logic stays bounded so failure visibility remains timely and operator trust posture stays explicit.
---

<objective>
Implement the live outbound Telegram delivery path for Phase 6.

Purpose: move the existing Telegram alert rendering into a production-facing runtime adapter that can send messages, retry briefly, and record delivery outcomes for monitoring.
Output: a Telegram transport/runtime delivery seam, delivery-attempt recording, ops-health integration, and automated tests for send success, bounded retry, and failure reporting.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-CONTEXT.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-RESEARCH.md
@.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce Telegram transport and runtime delivery services over existing rendered message contracts</name>
  <files>backend/app/alerts/telegram_runtime.py, backend/app/alerts/telegram_transport.py, backend/tests/operator_workflow/test_telegram_runtime_delivery.py</files>
  <action>Create the production-facing outbound Telegram delivery seam for Phase 6. It must accept existing `RenderedTelegramMessage` outputs, send them through a transport abstraction, and keep transport failures isolated from the pure rendering/domain modules. The runtime service should expose send results in a form that higher layers can use without importing Telegram-specific payload structures into the approval or broker workflow.</action>
  <verify>cd backend && uv run pytest tests/operator_workflow/test_telegram_runtime_delivery.py -q</verify>
  <done>A runtime delivery service exists over the current renderer outputs, successful sends are observable without leaking transport details into domain code, and tests prove the new seam can deliver Telegram messages through fake transport adapters.</done>
</task>

<task type="auto">
  <name>Task 2: Add bounded retry behavior and feed delivery outcomes into ops health and incident reporting</name>
  <files>backend/app/alerts/telegram_runtime.py, backend/app/ops/alert_delivery_health.py, backend/app/ops/incident_log.py, backend/tests/operator_workflow/test_telegram_runtime_delivery.py, backend/tests/ops_dashboard/test_telegram_runtime_failures.py</files>
  <action>Extend the runtime delivery seam with the chosen bounded retry posture. Repeated failures should record `AlertDeliveryAttempt` data that the existing ops health and incident log services can surface, while recovery after a later success should also be observable. Keep the retry window intentionally brief so live delivery failures become visible quickly rather than being hidden behind aggressive resend loops.</action>
  <verify>cd backend && uv run pytest tests/operator_workflow/test_telegram_runtime_delivery.py tests/ops_dashboard/test_telegram_runtime_failures.py -q</verify>
  <done>Telegram runtime delivery retries only briefly before surfacing failure, delivery attempts feed the existing health/incident reporting seams, and tests prove both failure streaks and recovery summaries can be derived from runtime send outcomes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/operator_workflow/test_telegram_runtime_delivery.py tests/ops_dashboard/test_telegram_runtime_failures.py -q
- [ ] Outbound Telegram delivery consumes rendered message contracts rather than re-implementing alert formatting
- [ ] Retry behavior is bounded and does not hide repeated failures
- [ ] Delivery attempts update the existing ops-health and incident-reporting seams
- [ ] Transport details stay isolated from approval workflow and paper-broker logic
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 6 has a live outbound Telegram delivery seam that closes the render-only gap for FLOW-01
</success_criteria>

<output>
After completion, create `.planning/phases/06-telegram-runtime-delivery-and-callback-wiring/06-01-SUMMARY.md`
</output>

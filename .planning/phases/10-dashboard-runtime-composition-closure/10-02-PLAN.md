---
phase: 10-dashboard-runtime-composition-closure
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - backend/app/main.py
  - backend/app/api/dashboard_runtime.py
  - backend/app/ops/incident_log.py
  - backend/app/audit/lifecycle_log.py
  - backend/tests/dashboard/test_dashboard_serving.py
  - backend/tests/dashboard/test_dashboard_review_and_logs.py
  - backend/tests/dashboard/test_dashboard_overview.py
  - backend/tests/dashboard/test_dashboard_runtime_state.py
  - backend/tests/audit_review/test_trade_review_groups.py
  - backend/tests/audit_review/test_pnl_summary.py
autonomous: true
requirements:
  - FLOW-06
user_setup: []
must_haves:
  truths:
    - The default served dashboard overview, logs, trades, and P&amp;L routes render from shared runtime-owned monitoring and review sources rather than placeholder tuples or test-only snapshots.
    - Trade review and paper P&amp;L are built from the same canonical lifecycle event source used by the runtime operator workflow.
    - Overview and incident sections reflect real composed trust, scanner, and alert-delivery state through the default app boundary.
    - Milestone verification proves the served dashboard is operator-usable through the default runtime composition path, including stale-snapshot fallback after real source wiring.
  artifacts:
    - backend/app/api/dashboard_runtime.py
    - backend/app/ops/incident_log.py
    - backend/app/audit/lifecycle_log.py
    - backend/tests/dashboard/test_dashboard_serving.py
    - backend/tests/dashboard/test_dashboard_review_and_logs.py
    - backend/tests/dashboard/test_dashboard_overview.py
    - backend/tests/dashboard/test_dashboard_runtime_state.py
    - backend/tests/audit_review/test_trade_review_groups.py
    - backend/tests/audit_review/test_pnl_summary.py
  key_links:
    - `backend/app/api/dashboard_runtime.py` must assemble overview and incident sections from `backend/app/ops/incident_log.py` plus the existing monitoring read-model services instead of placeholder tuples.
    - `backend/app/api/dashboard_runtime.py` must build review and paper P&amp;L from the shared `backend/app/audit/lifecycle_log.py` event source consumed by the existing audit read models.
    - `backend/tests/dashboard/test_dashboard_review_and_logs.py`, `backend/tests/dashboard/test_dashboard_overview.py`, `backend/tests/dashboard/test_dashboard_serving.py`, `backend/tests/audit_review/test_trade_review_groups.py`, and `backend/tests/audit_review/test_pnl_summary.py` are the executable proof artifacts for `10-02-01` and `10-02-02`.
---

<objective>
Close the remaining default dashboard integration gap by wiring real monitoring and review sources into the served runtime boundary.

Purpose: make the shipped dashboard routes provefully reflect shared runtime-owned trust, incident, review, and P&amp;L state through the default app composition.
Output: default snapshot assembly over canonical runtime sources, served-boundary verification for overview/logs/trades/P&amp;L, and milestone-ready evidence that `FLOW-06` is fully integrated.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-dashboard-runtime-composition-closure/10-RESEARCH.md
@.planning/phases/10-dashboard-runtime-composition-closure/10-VALIDATION.md
@.planning/phases/10-dashboard-runtime-composition-closure/10-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build default dashboard snapshots from shared monitoring and lifecycle runtime sources</name>
  <files>backend/app/api/dashboard_runtime.py, backend/app/ops/incident_log.py, backend/app/audit/lifecycle_log.py, backend/tests/dashboard/test_dashboard_runtime_state.py, backend/tests/dashboard/test_dashboard_overview.py, backend/tests/dashboard/test_dashboard_review_and_logs.py, backend/tests/audit_review/test_trade_review_groups.py, backend/tests/audit_review/test_pnl_summary.py</files>
  <action>Wire the runtime composition seam from Plan 01 into the actual operators’ data sources. The default snapshot builder should derive overview and incident content from shared trust, scanner-loop, alert-delivery, and system-event state, and it should derive trade review plus paper P&amp;L from the same `LifecycleLog` used by runtime workflow code. Reuse the existing read-model services directly, keep dashboard routes thin, and avoid introducing a second review-event store or dashboard-only monitoring state.</action>
  <verify>cd backend && uv run pytest tests/dashboard/test_dashboard_overview.py tests/dashboard/test_dashboard_review_and_logs.py -q</verify>
  <done>The default snapshot provider assembles overview, logs, trades, and P&amp;L from shared runtime-owned sources, and review/P&amp;L remain traceable to the canonical lifecycle event log.</done>
</task>

<task type="auto">
  <name>Task 2: Prove milestone-ready served dashboard behavior through the default app boundary</name>
  <files>backend/app/main.py, backend/app/api/dashboard_runtime.py, backend/tests/dashboard/test_dashboard_serving.py, backend/tests/dashboard/test_dashboard_review_and_logs.py, backend/tests/dashboard/test_dashboard_overview.py</files>
  <action>Upgrade the served-boundary tests so the milestone proof starts from the default app composition path rather than injected synthetic snapshot factories. Verify that overview, logs, trades, and P&amp;L routes are reachable after config-backed login, that operator-visible content reflects real composed runtime state, and that stale fallback still preserves the last successful dashboard snapshot after real source wiring. Keep verification focused on `FLOW-06` closure rather than dashboard redesign or broader milestone-audit document work.</action>
  <verify>cd backend && uv run pytest tests/dashboard/test_dashboard_serving.py -k "dashboard or login or trades or pnl" -q</verify>
  <done>Milestone evidence shows the default served app can authenticate, compose real runtime dashboard data, serve overview/logs/trades/P&amp;L end to end, and preserve stale fallback through the shipped runtime boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Task 1 smoke: `cd backend && uv run pytest tests/dashboard/test_dashboard_overview.py tests/dashboard/test_dashboard_review_and_logs.py -q`
- [ ] Task 1 lifecycle sample: `cd backend && uv run pytest tests/audit_review/test_trade_review_groups.py tests/audit_review/test_pnl_summary.py -q`
- [ ] Task 2 smoke: `cd backend && uv run pytest tests/dashboard/test_dashboard_serving.py -k "dashboard or login or trades or pnl" -q`
- [ ] cd backend && uv run pytest tests/dashboard/test_dashboard_serving.py tests/dashboard/test_dashboard_review_and_logs.py tests/dashboard/test_dashboard_overview.py tests/dashboard/test_dashboard_runtime_state.py tests/audit_review/test_trade_review_groups.py tests/audit_review/test_pnl_summary.py -q
- [ ] Overview, logs, trade review, and P&amp;L are served through the default app boundary after config-backed login
- [ ] Review and P&amp;L derive from the shared lifecycle event source used by runtime workflow code
- [ ] Monitoring sections reflect real composed runtime trust, scanner, alert-delivery, and incident state
- [ ] Stale fallback still works after the default snapshot path is wired to real runtime-owned sources
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 10 closes the remaining default dashboard composition gap and gives milestone-ready served-boundary proof for `FLOW-06`
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-runtime-composition-closure/10-02-SUMMARY.md`
</output>

---
phase: 10-dashboard-runtime-composition-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/main.py
  - backend/app/config.py
  - backend/app/api/dashboard_runtime.py
  - backend/app/api/dashboard_auth.py
  - backend/tests/dashboard/test_dashboard_auth.py
  - backend/tests/dashboard/test_dashboard_runtime_state.py
  - backend/tests/dashboard/test_dashboard_serving.py
autonomous: true
requirements:
  - FLOW-06
user_setup: []
must_haves:
  truths:
    - The default served app composes dashboard auth from runtime configuration instead of requiring ad hoc injected `DashboardAuthSettings`.
    - Missing dashboard password or session-secret configuration still fails closed, but a configured default app no longer returns `503 Dashboard access is not configured.` by construction.
    - The dashboard snapshot provider used by `create_app()` is backed by an explicit runtime-owned composition seam rather than `_build_default_snapshot()` assembling synthetic empty tuples internally.
    - Stale fallback behavior remains intact after default runtime composition is introduced.
  artifacts:
    - backend/app/main.py
    - backend/app/config.py
    - backend/app/api/dashboard_runtime.py
    - backend/app/api/dashboard_auth.py
    - backend/tests/dashboard/test_dashboard_auth.py
    - backend/tests/dashboard/test_dashboard_runtime_state.py
    - backend/tests/dashboard/test_dashboard_serving.py
  key_links:
    - `backend/app/main.py:create_app()` must compose the default dashboard runtime seam from `backend/app/api/dashboard_runtime.py` instead of falling back to `_build_default_snapshot()` placeholder assembly.
    - `backend/app/main.py:create_app()` must resolve default `DashboardAuthSettings` from `backend/app/config.py` and pass them into `backend/app/api/dashboard_auth.py` without changing the route-layer auth contract.
    - `backend/tests/dashboard/test_dashboard_runtime_state.py`, `backend/tests/dashboard/test_dashboard_auth.py`, and `backend/tests/dashboard/test_dashboard_serving.py` are the concrete served-boundary proof artifacts for `10-01-01` and `10-01-02`.
---

<objective>
Establish a milestone-ready default dashboard runtime composition path for Phase 10.

Purpose: make `create_app()` assemble usable dashboard auth and a real runtime snapshot provider without relying on test-only injection.
Output: a default app composition seam that resolves dashboard auth from configuration, owns shared dashboard runtime dependencies explicitly, and preserves stale-safe snapshot behavior for `FLOW-06`.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-dashboard-runtime-composition-closure/10-RESEARCH.md
@.planning/phases/10-dashboard-runtime-composition-closure/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace synthetic default dashboard snapshot assembly with an explicit runtime composition seam</name>
  <files>backend/app/main.py, backend/app/api/dashboard_runtime.py, backend/tests/dashboard/test_dashboard_runtime_state.py, backend/tests/dashboard/test_dashboard_serving.py</files>
  <action>Introduce one narrow runtime-owned dashboard composition object or factory that `create_app()` can use by default. It should own the shared sources needed for dashboard snapshot assembly, expose a clear path for later runtime state updates, and stop `_build_default_snapshot()` from manufacturing overview, incidents, trade review, and P&amp;L from hard-coded empty tuples. Keep `DashboardRuntimeSnapshotProvider` as the route-facing seam, keep stale fallback semantics unchanged, and avoid pulling snapshot assembly into route handlers.</action>
  <verify>cd backend && uv run pytest tests/dashboard/test_dashboard_runtime_state.py -k "default or stale" -q</verify>
  <done>The default app uses an explicit runtime composition seam for dashboard snapshots, the provider still supports stale fallback, and the served app no longer depends on synthetic placeholder assembly as its default behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Load dashboard auth settings from app configuration and keep fail-closed behavior for missing config</name>
  <files>backend/app/main.py, backend/app/config.py, backend/app/api/dashboard_auth.py, backend/tests/dashboard/test_dashboard_auth.py, backend/tests/dashboard/test_dashboard_serving.py</files>
  <action>Add the minimum environment-backed app configuration needed for dashboard password and session secret, then wire `create_app()` to resolve `DashboardAuthSettings` from that configuration by default. Preserve dashboard-scoped auth only, keep the route layer contract unchanged, and prove both paths: a configured default app can issue a session cookie and serve the dashboard, while an unconfigured app still fails closed with a clear operator-facing response.</action>
  <verify>cd backend && uv run pytest tests/dashboard/test_dashboard_auth.py -k "session or fail_closed or login" -q</verify>
  <done>The default served app can bootstrap dashboard auth from configuration, successful login creates the existing session cookie flow, and missing configuration still fails closed without reopening the `503` gap for configured milestone runtime usage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Task 1 smoke: `cd backend && uv run pytest tests/dashboard/test_dashboard_runtime_state.py -k "default or stale" -q`
- [ ] Task 2 smoke: `cd backend && uv run pytest tests/dashboard/test_dashboard_auth.py -k "session or fail_closed or login" -q`
- [ ] Plan 01 served-boundary sample: `cd backend && uv run pytest tests/dashboard/test_dashboard_serving.py -k "login or dashboard" -q`
- [ ] cd backend && uv run pytest tests/dashboard/test_dashboard_auth.py tests/dashboard/test_dashboard_runtime_state.py tests/dashboard/test_dashboard_serving.py -q
- [ ] `create_app()` composes dashboard auth and snapshot dependencies by default instead of requiring test-only injection
- [ ] Missing dashboard auth config still fails closed with a dashboard-scoped response
- [ ] The default dashboard snapshot path no longer manufactures empty review and monitoring inputs internally
- [ ] Stale fallback still preserves the last successful snapshot when refresh fails
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 10 has a default runtime/auth composition path that can support milestone-ready served dashboard review for `FLOW-06`
</success_criteria>

<output>
After completion, create `.planning/phases/10-dashboard-runtime-composition-closure/10-01-SUMMARY.md`
</output>

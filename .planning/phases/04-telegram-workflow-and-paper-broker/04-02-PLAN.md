---
phase: 04-telegram-workflow-and-paper-broker
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - backend/app/paper/models.py
  - backend/app/paper/broker.py
  - backend/app/paper/exits.py
  - backend/tests/paper_broker/test_entry_handling.py
  - backend/tests/paper_broker/test_exit_handling.py
autonomous: true
requirements:
  - FLOW-04
  - FLOW-05
  - RISK-06
  - RISK-07
user_setup: []
must_haves:
  truths:
    - The system can turn an approved Phase 4 operator decision into a simulated paper trade with configurable slippage and a clean path for optional partial-fill support.
    - Open paper trades are managed by deterministic stop, target, and responsive early-exit rules rather than ad hoc manual logic.
    - Operator overrides on open trades are limited to the chosen v1 actions: close immediately, adjust stop, and adjust target.
  artifacts:
    - backend/app/paper/models.py
    - backend/app/paper/broker.py
    - backend/app/paper/exits.py
    - backend/tests/paper_broker/test_entry_handling.py
    - backend/tests/paper_broker/test_exit_handling.py
  key_links:
    - Paper-trade entry consumes approved operator-decision records from the Telegram workflow rather than raw message payloads.
    - Exit handling remains separate from Telegram messaging so the broker can manage trades deterministically while Phase 4 messaging stays material-events-only.
---

<objective>
Implement paper broker state model, entry handling, and exit handling for Phase 4.

Purpose: create the simulated trade engine that opens approved paper trades, applies fill assumptions, manages open-trade exits, and supports the narrow operator overrides chosen for v1.
Output: paper-trade domain models, entry/fill simulation, exit handling, override actions, and automated paper-broker tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-telegram-workflow-and-paper-broker/04-CONTEXT.md
@.planning/phases/04-telegram-workflow-and-paper-broker/04-RESEARCH.md
@.planning/phases/04-telegram-workflow-and-paper-broker/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement paper-trade state models and simulated entry handling</name>
  <files>backend/app/paper/models.py, backend/app/paper/broker.py, backend/tests/paper_broker/test_entry_handling.py</files>
  <action>Implement the core paper-broker domain for Phase 4. This must accept approved operator decisions, create deterministic paper-trade state, and simulate entry fills using configurable slippage while preserving a clean hook for optional partial-fill behavior that remains disabled by default in v1. Keep live execution and venue-specific order models out of scope.</action>
  <verify>cd backend && uv run pytest tests/paper_broker/test_entry_handling.py -q</verify>
  <done>Approved entries can open simulated paper trades with explicit fill assumptions, slippage is configurable, and tests prove the broker supports current defaults plus future partial-fill extension points.</done>
</task>

<task type="auto">
  <name>Task 2: Implement automatic exit handling and open-trade operator overrides</name>
  <files>backend/app/paper/exits.py, backend/app/paper/broker.py, backend/tests/paper_broker/test_exit_handling.py</files>
  <action>Implement the chosen Phase 4 exit posture for open paper trades. The broker must support automatic hard-stop, target, and responsive early-exit behavior while allowing only the selected operator overrides: close immediately, adjust stop, and adjust target. Keep exit-rule evaluation deterministic and do not add chatty Telegram messaging concerns into broker logic.</action>
  <verify>cd backend && uv run pytest tests/paper_broker/test_exit_handling.py -q</verify>
  <done>Open paper trades can be closed automatically by the configured protective and responsive rules, override actions are constrained to the v1 scope, and tests prove exits and adjustments behave coherently.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/paper_broker/test_entry_handling.py tests/paper_broker/test_exit_handling.py -q
- [ ] Approved decisions can open simulated paper trades
- [ ] Slippage is configurable and partial-fill support remains optional/off by default
- [ ] Exit handling includes stop, target, and responsive early-exit behavior
- [ ] Override actions are limited to close, stop adjustment, and target adjustment
- [ ] No live-execution or venue-specific order behavior is introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 4 has a deterministic paper broker ready for risk-gate integration
</success_criteria>

<output>
After completion, create `.planning/phases/04-telegram-workflow-and-paper-broker/04-02-SUMMARY.md`
</output>

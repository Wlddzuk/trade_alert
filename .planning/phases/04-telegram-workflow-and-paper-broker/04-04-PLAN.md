---
phase: 04-telegram-workflow-and-paper-broker
plan: 04
type: execute
wave: 4
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - backend/app/audit/models.py
  - backend/app/audit/lifecycle_log.py
  - backend/app/audit/trade_review.py
  - backend/app/alerts/approval_workflow.py
  - backend/app/paper/broker.py
  - backend/tests/paper_broker/test_lifecycle_audit.py
  - backend/tests/paper_broker/test_trade_review_log.py
autonomous: true
requirements:
  - FLOW-02
  - FLOW-03
  - FLOW-04
  - FLOW-05
phase_goal_trace:
  - P4-LIFECYCLE
user_setup: []
must_haves:
  truths:
    - This plan is the explicit Phase 4 trace for the roadmap requirement to persist full paper-trade lifecycle events for downstream review.
    - The system records immutable lifecycle events for surfaced setups, operator decisions, paper fills, adjustments, exits, and results with UTC timestamps.
    - Lifecycle data is append-only and reviewable without relying on mutable in-memory trade state.
    - Downstream review surfaces can reconstruct a paper trade and its operator actions from the persisted lifecycle stream.
  artifacts:
    - backend/app/audit/models.py
    - backend/app/audit/lifecycle_log.py
    - backend/app/audit/trade_review.py
    - backend/tests/paper_broker/test_lifecycle_audit.py
    - backend/tests/paper_broker/test_trade_review_log.py
  key_links:
    - Lifecycle events are emitted from Telegram workflow, risk decisions, and paper-broker transitions so later audit/review surfaces do not need to infer missing history.
    - Review helpers derive summaries from immutable events rather than inventing a separate source of truth for completed paper trades.
---

<objective>
Persist full paper-trade lifecycle events for downstream review in Phase 4.

Purpose: make every surfaced setup, operator action, and paper-trade outcome reconstructable so Phase 5 can build audit and review surfaces on stable ground.
Output: immutable lifecycle event models, append-only recording, review-ready summary helpers, and automated audit-log tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-telegram-workflow-and-paper-broker/04-CONTEXT.md
@.planning/phases/04-telegram-workflow-and-paper-broker/04-RESEARCH.md
@.planning/phases/04-telegram-workflow-and-paper-broker/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement immutable lifecycle event models and append-only recording</name>
  <files>backend/app/audit/models.py, backend/app/audit/lifecycle_log.py, backend/tests/paper_broker/test_lifecycle_audit.py</files>
  <action>Implement the append-only lifecycle event layer for Phase 4. Event records must cover surfaced signals, blocked/rejected outcomes for already-surfaced symbols, operator approvals/rejections/adjustments, simulated fills, exit triggers, manual overrides, and final results. All lifecycle timestamps must be UTC-safe and immutable. Do not add dashboard rendering in this plan.</action>
  <verify>cd backend && uv run pytest tests/paper_broker/test_lifecycle_audit.py -q</verify>
  <done>Lifecycle event models and recording exist, the event stream is append-only and immutable, and tests prove core Phase 4 workflow events are captured with UTC-safe timestamps.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate lifecycle recording across workflow and expose review-ready trade summaries</name>
  <files>backend/app/audit/trade_review.py, backend/app/alerts/approval_workflow.py, backend/app/paper/broker.py, backend/tests/paper_broker/test_trade_review_log.py</files>
  <action>Integrate lifecycle recording across the Telegram workflow, risk gating, and paper-broker transitions so downstream review can reconstruct each paper trade and its operator decisions. Add review-ready summary helpers over the immutable event stream, but keep Phase 5 UI/reporting work deferred.</action>
  <verify>cd backend && uv run pytest tests/paper_broker/test_trade_review_log.py -q</verify>
  <done>Phase 4 emits lifecycle events from the alert/approval/broker flow end-to-end, review helpers can reconstruct a trade and its result from immutable events, and tests prove downstream review no longer depends on mutable live state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/paper_broker/test_lifecycle_audit.py tests/paper_broker/test_trade_review_log.py -q
- [ ] Surfaced-signal, operator-decision, fill, adjustment, exit, and result events are recorded immutably
- [ ] Timestamps are UTC-safe
- [ ] Review helpers derive summaries from the lifecycle stream rather than mutable trade state
- [ ] Phase 5 dashboard/reporting work is not pulled into this plan
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 4 leaves Phase 5 with stable audit and review-ready lifecycle data
</success_criteria>

<output>
After completion, create `.planning/phases/04-telegram-workflow-and-paper-broker/04-04-SUMMARY.md`
</output>

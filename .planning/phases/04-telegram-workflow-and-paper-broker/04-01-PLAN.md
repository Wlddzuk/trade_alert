---
phase: 04-telegram-workflow-and-paper-broker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/alerts/models.py
  - backend/app/alerts/telegram_renderer.py
  - backend/app/alerts/delivery_state.py
  - backend/app/alerts/approval_workflow.py
  - backend/tests/operator_workflow/test_telegram_alerts.py
  - backend/tests/operator_workflow/test_delivery_state.py
  - backend/tests/operator_workflow/test_operator_decisions.py
  - backend/tests/operator_workflow/test_open_trade_messages.py
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
  - FLOW-05
user_setup: []
must_haves:
  truths:
    - The system can project ranked Phase 3 strategy state into watch-only, actionable, blocked, and rejected Telegram alerts without re-implementing scanner logic.
    - A watched setup that becomes trigger-ready emits a fresh actionable alert rather than mutating the earlier watch alert into the approval surface.
    - Operator entry approval supports both one-tap default approval and a separate stop/target adjustment path before paper-trade creation.
    - Telegram remains the primary operator surface after entry by supporting material-event trade messages plus the chosen open-trade controls for close, stop adjustment, and target adjustment.
    - Entry price remains tied to the alert proposal in v1 and cannot be edited through the approval workflow.
  artifacts:
    - backend/app/alerts/models.py
    - backend/app/alerts/telegram_renderer.py
    - backend/app/alerts/delivery_state.py
    - backend/app/alerts/approval_workflow.py
    - backend/tests/operator_workflow/test_telegram_alerts.py
    - backend/tests/operator_workflow/test_delivery_state.py
    - backend/tests/operator_workflow/test_operator_decisions.py
    - backend/tests/operator_workflow/test_open_trade_messages.py
  key_links:
    - Telegram alert projection consumes Phase 3 `StrategyProjection` outputs rather than raw candidate rows.
    - Alert delivery state distinguishes the original watch alert from the later fresh actionable alert and suppresses blocked/rejected follow-ups for symbols that were never surfaced.
    - Approval decisions are represented as domain records that later paper-broker code can consume without being tied to Telegram transport details.
---

<objective>
Build Telegram alert delivery and approval workflow for Phase 4.

Purpose: turn the ranked strategy scanner into a Telegram-led operator loop while keeping alert transport separate from operator-decision semantics.
Output: alert models and rendering for watch/actionable/blocked/rejected and open-trade material-event states, plus operator approve/reject/adjust/override decision handling and automated Telegram workflow tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-telegram-workflow-and-paper-broker/04-CONTEXT.md
@.planning/phases/04-telegram-workflow-and-paper-broker/04-RESEARCH.md
@.planning/phases/04-telegram-workflow-and-paper-broker/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Telegram alert projection, delivery-state handling, and rendering for watch/actionable/blocked/rejected progression</name>
  <files>backend/app/alerts/models.py, backend/app/alerts/telegram_renderer.py, backend/app/alerts/delivery_state.py, backend/tests/operator_workflow/test_telegram_alerts.py, backend/tests/operator_workflow/test_delivery_state.py</files>
  <action>Implement the Phase 4 Telegram alert surface over ranked strategy projections. Alert projection must support the decided watch-only, actionable, blocked, and rejected states without recomputing scanner validity or ranking. Delivery-state handling must make the watch-to-trigger-ready progression explicit so a watched setup emits a fresh actionable alert rather than editing the earlier watch alert. Rendering should include catalyst context, setup status, entry, stop, target, and score/rank, while remaining transport-focused and free of paper-broker state transitions. Blocked or rejected notices must only be representable for symbols that were already surfaced in Telegram.</action>
  <verify>cd backend && uv run pytest tests/operator_workflow/test_telegram_alerts.py tests/operator_workflow/test_delivery_state.py -q</verify>
  <done>Telegram alert models, delivery-state rules, and rendering exist for the chosen Phase 4 pre-entry message classes, and tests prove watch-to-actionable progression uses a fresh actionable alert while blocked/rejected follow-ups stay limited to already-surfaced symbols.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Telegram operator inputs for pre-entry approval and open-trade material-event controls</name>
  <files>backend/app/alerts/approval_workflow.py, backend/app/alerts/telegram_renderer.py, backend/tests/operator_workflow/test_operator_decisions.py, backend/tests/operator_workflow/test_open_trade_messages.py</files>
  <action>Implement the Telegram-side operator workflow for both pre-entry and open-trade interactions. The workflow must support one-tap default approval, explicit reject, and a separate pre-entry stop/target adjustment path while keeping entry price tied to the alert proposal in v1. It must also support the chosen material-event open-trade message classes and operator inputs for close, adjust stop, and adjust target. Decision records should stay transport-independent so later paper-broker code can consume them directly. Do not implement broker-side execution in this plan.</action>
  <verify>cd backend && uv run pytest tests/operator_workflow/test_operator_decisions.py tests/operator_workflow/test_open_trade_messages.py -q</verify>
  <done>Operator decisions are represented as explicit approval/rejection/adjustment/override records, Telegram can surface trade-opened/adjusted/closed messages without becoming chatty, and tests prove both the fast default path and open-trade controls work as intended.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/operator_workflow/test_telegram_alerts.py tests/operator_workflow/test_delivery_state.py tests/operator_workflow/test_operator_decisions.py tests/operator_workflow/test_open_trade_messages.py -q
- [ ] Watch-only setups are not approval-capable
- [ ] Trigger-ready setups can surface as actionable alerts
- [ ] A watched setup that becomes trigger-ready emits a fresh actionable alert rather than editing the prior watch alert
- [ ] Blocked and rejected notices are only available for already-surfaced symbols
- [ ] Approval supports one-tap defaults plus separate stop/target adjustment
- [ ] Telegram supports material-event open-trade messages and the chosen close/adjust-stop/adjust-target inputs
- [ ] Entry price stays tied to the alert proposal
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 4 has a Telegram-led operator loop foundation ready for paper-trade integration
</success_criteria>

<output>
After completion, create `.planning/phases/04-telegram-workflow-and-paper-broker/04-01-SUMMARY.md`
</output>

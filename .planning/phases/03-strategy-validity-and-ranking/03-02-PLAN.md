---
phase: 03-strategy-validity-and-ranking
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - backend/app/providers/base.py
  - backend/app/providers/models.py
  - backend/app/providers/polygon_adapter.py
  - backend/app/scanner/history.py
  - backend/app/scanner/trigger_policy.py
  - backend/app/scanner/trigger_logic.py
  - backend/app/scanner/invalidation.py
  - backend/tests/scanner_strategy/test_trigger_logic.py
  - backend/tests/scanner_strategy/test_invalidations.py
autonomous: true
requirements:
  - SIG-03
  - SIG-04
user_setup: []
must_haves:
  truths:
    - The system prefers `15-second` trigger bars when available and falls back explicitly to `1-minute` bars when they are not.
    - Trigger logic fires on the first intrabar break of the prior candle high after a valid pullback and does not require a specific bullish candle pattern.
    - Invalid signals are suppressed when configured catalyst, volume, momentum, halt, or repeated-breakout rules fail before trigger.
  artifacts:
    - backend/app/scanner/trigger_policy.py
    - backend/app/scanner/trigger_logic.py
    - backend/app/scanner/invalidation.py
    - backend/tests/scanner_strategy/test_trigger_logic.py
    - backend/tests/scanner_strategy/test_invalidations.py
  key_links:
    - Trigger evaluation consumes Phase 3 setup-valid outputs and normalized trigger-bar inputs rather than embedding trigger rules in the Phase 2 feed service.
    - Timeframe fallback is explicit and testable instead of silently using whatever bar resolution happens to exist.
---

<objective>
Implement trigger logic and invalidation handling for the Phase 3 strategy scanner.

Purpose: move from “valid setup” to “trigger-ready or invalidated” behavior using the user-chosen trigger semantics, timeframe fallback, and broken-momentum rules.
Output: trigger-timeframe policy, normalized trigger-bar access or fallback path, trigger evaluation, invalidation handling, and automated trigger/invalidation tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-strategy-validity-and-ranking/03-CONTEXT.md
@.planning/phases/03-strategy-validity-and-ranking/03-RESEARCH.md
@.planning/phases/03-strategy-validity-and-ranking/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement trigger-timeframe policy and normalized trigger-bar access</name>
  <files>backend/app/providers/base.py, backend/app/providers/models.py, backend/app/providers/polygon_adapter.py, backend/app/scanner/history.py, backend/app/scanner/trigger_policy.py, backend/tests/scanner_strategy/test_trigger_logic.py</files>
  <action>Implement the normalized trigger-timeframe path for Phase 3. This work must make the `15-second` preferred / `1-minute` fallback policy explicit and keep it behind internal provider and scanner abstractions rather than hard-coding Polygon-specific logic into trigger evaluation. Handle the current minute-based bar-model gap directly so the trigger layer can request the preferred resolution and observe a deterministic fallback when needed.</action>
  <verify>cd backend && uv run pytest tests/scanner_strategy/test_trigger_logic.py -q</verify>
  <done>Trigger-timeframe selection is explicit, normalized trigger-bar inputs are available through the scanner/provider boundary, and tests prove `15-second` preference plus `1-minute` fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement first-break trigger evaluation and Phase 3 invalidation rules</name>
  <files>backend/app/scanner/trigger_logic.py, backend/app/scanner/invalidation.py, backend/tests/scanner_strategy/test_trigger_logic.py, backend/tests/scanner_strategy/test_invalidations.py</files>
  <action>Implement Phase 3 trigger and invalidation behavior over valid setups. Trigger logic must fire on the first intrabar break above the prior candle high after a valid pullback, while bullish candle confirmation remains a score/context boost only. Invalidation handling must cover stale or contradictory catalyst, weak relative volume, pullback too deep, broken momentum via pullback-low break or loss of `VWAP` and `20 EMA`, trading halt/LULD conditions, and exhaustion of the move after two visible failed breakout attempts. Do not add Phase 4 risk-gating concerns such as spread, liquidity, stop-distance, or cutoff-time rejection here.</action>
  <verify>cd backend && uv run pytest tests/scanner_strategy/test_trigger_logic.py tests/scanner_strategy/test_invalidations.py -q</verify>
  <done>Trigger-ready candidates can trigger deterministically on the chosen first-break rule, invalidations suppress dead or broken moves before trigger, and tests cover timeframe fallback, first-break behavior, and the configured invalidation set.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/scanner_strategy/test_trigger_logic.py tests/scanner_strategy/test_invalidations.py -q
- [ ] `15-second` preferred / `1-minute` fallback is explicit and test-covered
- [ ] Trigger fires on first intrabar break above prior candle high after a valid pullback
- [ ] Bullish confirmation is not a hard trigger gate
- [ ] Broken momentum, stale/contradictory catalyst, halt/LULD, weak RVOL, deep pullback, and repeated failed breakouts suppress invalid signals
- [ ] No Phase 4 risk-gating rules are smuggled into this plan
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 has trigger-ready and invalidated state behavior that matches the chosen product rules
</success_criteria>

<output>
After completion, create `.planning/phases/03-strategy-validity-and-ranking/03-02-SUMMARY.md`
</output>

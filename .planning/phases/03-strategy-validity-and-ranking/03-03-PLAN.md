---
phase: 03-strategy-validity-and-ranking
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - backend/app/scanner/strategy_ranking.py
  - backend/app/scanner/strategy_tags.py
  - backend/app/scanner/strategy_projection.py
  - backend/app/scanner/feed_service.py
  - backend/tests/scanner_strategy/test_ranking.py
  - backend/tests/scanner_strategy/test_stage_tags.py
autonomous: true
requirements:
  - SCAN-06
  - SCAN-05
user_setup: []
must_haves:
  truths:
    - The system assigns each candidate a numeric `0-100` score and a strategy-stage explanation without breaking the existing one-row-per-symbol feed model.
    - Valid setups rank above invalid ones, while invalid candidates remain visible for context with their primary invalid reason.
    - Ranking remains quality-first, rewarding validity, freshness, move strength, and relative-volume context rather than any single factor dominating.
  artifacts:
    - backend/app/scanner/strategy_ranking.py
    - backend/app/scanner/strategy_tags.py
    - backend/app/scanner/strategy_projection.py
    - backend/tests/scanner_strategy/test_ranking.py
    - backend/tests/scanner_strategy/test_stage_tags.py
  key_links:
    - Ranking consumes validity and trigger/invalidation state rather than recomputing raw strategy rules in the feed layer.
    - Feed ordering is replaced by strategy-aware ranking only after score and stage projection exist.
---

<objective>
Implement score/rank behavior and strategy-specific stage explanation for Phase 3.

Purpose: replace the provisional Phase 2 feed ordering with the chosen quality-first ranking model and make each row explain its current strategy stage clearly to the operator.
Output: numeric score model, stage-tag/supporting-reason projection, valid-first ordering integration, and automated ranking/tagging tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-strategy-validity-and-ranking/03-CONTEXT.md
@.planning/phases/03-strategy-validity-and-ranking/03-RESEARCH.md
@.planning/phases/03-strategy-validity-and-ranking/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the quality-first numeric score and valid-first ordering behavior</name>
  <files>backend/app/scanner/strategy_ranking.py, backend/app/scanner/feed_service.py, backend/tests/scanner_strategy/test_ranking.py</files>
  <action>Implement the Phase 3 numeric ranking model and integrate it into the live candidate feed. Score must be on a `0-100` scale, remain quality-first, and order rows by validity bucket before score so invalid candidates never outrank valid setups. Weighting may remain flexible, but it must reward valid pullback structure, catalyst freshness, move strength, and daily/short-term RVOL context. Do not let score replace or override the primary invalid-reason contract.</action>
  <verify>cd backend && uv run pytest tests/scanner_strategy/test_ranking.py -q</verify>
  <done>Rows receive a deterministic Phase 3 score, valid setups sort above invalid ones, and tests prove the feed ordering no longer depends on the provisional Phase 2 sort once strategy-state outputs exist.</done>
</task>

<task type="auto">
  <name>Task 2: Implement stage tags, supporting reasons, and strategy-aware row projection</name>
  <files>backend/app/scanner/strategy_tags.py, backend/app/scanner/strategy_projection.py, backend/tests/scanner_strategy/test_stage_tags.py</files>
  <action>Implement the strategy-stage explanation layer for Phase 3. Each row should expose one primary stage tag plus supporting reasons that reflect the current state, with initial direction such as `building`, `trigger_ready`, and `invalidated`. Keep label copy flexible but deterministic, preserve visibility of the primary invalid reason on invalid rows, and avoid adding Telegram-specific alert formatting or paper-trading state.</action>
  <verify>cd backend && uv run pytest tests/scanner_strategy/test_stage_tags.py -q</verify>
  <done>Strategy-aware row projection exposes score, stage tag, supporting reasons, and primary invalid reason coherently, and tests prove the tags remain aligned with the underlying validity and trigger state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/scanner_strategy/test_ranking.py tests/scanner_strategy/test_stage_tags.py -q
- [ ] Score is numeric on a `0-100` scale and remains quality-first
- [ ] Valid candidates sort above invalid ones
- [ ] Invalid candidates remain visible with their primary invalid reason
- [ ] Stage tags and supporting reasons reflect the current strategy state coherently
- [ ] Feed ordering no longer depends on provisional freshness-first logic once strategy ranking is available
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 produces a strategy-aware ranked feed ready for the Telegram and paper-trade workflow in Phase 4
</success_criteria>

<output>
After completion, create `.planning/phases/03-strategy-validity-and-ranking/03-03-SUMMARY.md`
</output>

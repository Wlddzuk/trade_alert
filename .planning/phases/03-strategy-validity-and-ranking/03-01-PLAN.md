---
phase: 03-strategy-validity-and-ranking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/scanner/news_linking.py
  - backend/app/scanner/strategy_defaults.py
  - backend/app/scanner/strategy_models.py
  - backend/app/scanner/context_features.py
  - backend/app/scanner/setup_validity.py
  - backend/tests/scanner_strategy/test_setup_validity.py
autonomous: true
requirements:
  - SCAN-05
  - SIG-01
  - SIG-02
user_setup: []
must_haves:
  truths:
    - The system can evaluate each candidate into a current `setup_valid` boolean plus one primary invalid reason without changing the Phase 2 row identity model.
    - Catalyst freshness for validity is measured from the first headline in the active catalyst cluster while the row still displays the latest related headline.
    - Default thresholds for catalyst age, move %, relative volume, pullback range, and hard context are configurable and not buried inside trigger or ranking code.
  artifacts:
    - backend/app/scanner/strategy_defaults.py
    - backend/app/scanner/strategy_models.py
    - backend/app/scanner/context_features.py
    - backend/app/scanner/setup_validity.py
    - backend/tests/scanner_strategy/test_setup_validity.py
  key_links:
    - Existing `LinkedNewsEvent.related_events` drives first-headline freshness without re-fetching raw news payloads.
    - Setup-valid evaluation consumes Phase 2 candidate rows plus new context features rather than leaking strategy rules into the Phase 2 metric or feed layers.
---

<objective>
Implement setup-valid rules and configurable Phase 3 strategy defaults.

Purpose: establish the strategy-state foundation that later trigger and ranking logic will depend on, while keeping the existing Phase 2 candidate-feed contracts stable.
Output: configurable strategy defaults, first-headline catalyst-age handling, context-feature calculations for validity, ordered setup-valid evaluation, and automated validity tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-strategy-validity-and-ranking/03-CONTEXT.md
@.planning/phases/03-strategy-validity-and-ranking/03-RESEARCH.md
@.planning/phases/03-strategy-validity-and-ranking/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement strategy defaults, first-headline freshness, and Phase 3 strategy-state models</name>
  <files>backend/app/scanner/news_linking.py, backend/app/scanner/strategy_defaults.py, backend/app/scanner/strategy_models.py, backend/tests/scanner_strategy/test_setup_validity.py</files>
  <action>Implement the Phase 3 default/config surface plus the strategy-state records that represent setup-validity outcomes. Add explicit first-headline catalyst-age handling over the existing linked-news model so validity uses the first headline in the active catalyst cluster while Phase 2 display semantics remain unchanged. Keep this work focused on defaults, timestamps, and state contracts. Do not implement trigger logic or ranking in this task.</action>
  <verify>cd backend && uv run pytest tests/scanner_strategy/test_setup_validity.py -q</verify>
  <done>Configurable Phase 3 defaults and strategy-state records exist, first-headline freshness is explicit and tested, and the codebase has a stable contract for `setup_valid` plus primary invalid reason.</done>
</task>

<task type="auto">
  <name>Task 2: Implement context-feature calculation and ordered setup-valid evaluation</name>
  <files>backend/app/scanner/context_features.py, backend/app/scanner/setup_validity.py, backend/tests/scanner_strategy/test_setup_validity.py</files>
  <action>Implement deterministic context-feature calculation and the ordered Phase 3 setup-valid evaluator. This must enforce the configured catalyst age, move %, daily RVOL, short-term RVOL, pullback range, and hard context rules (`VWAP` plus `9 EMA > 20 EMA`) while returning a primary invalid reason when validity fails. Keep pullback-volume quality, `200 EMA`, and `MACD` out of the hard gate. Do not add trigger or score behavior in this plan.</action>
  <verify>cd backend && uv run pytest tests/scanner_strategy/test_setup_validity.py -q</verify>
  <done>Each candidate can be deterministically evaluated into a current validity state with one dominant invalid reason, and automated tests cover fresh/expired catalyst, threshold failures, and pullback/context checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/scanner_strategy/test_setup_validity.py -q
- [ ] `setup_valid` exists as a boolean with one primary invalid reason when false
- [ ] Catalyst freshness uses first-headline age, not latest displayed headline age
- [ ] Thresholds for catalyst age, move %, RVOL, and pullback range are configurable
- [ ] Pullback-volume quality, `200 EMA`, and `MACD` do not become hard gates
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 has a stable strategy-state and validity foundation ready for trigger and ranking work
</success_criteria>

<output>
After completion, create `.planning/phases/03-strategy-validity-and-ranking/03-01-SUMMARY.md`
</output>

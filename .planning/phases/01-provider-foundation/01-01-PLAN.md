---
phase: 01-provider-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/app/config.py
  - backend/app/providers/base.py
  - backend/app/providers/errors.py
  - backend/app/providers/models.py
  - backend/app/providers/polygon_adapter.py
  - backend/app/providers/benzinga_adapter.py
  - backend/app/ingest/market_ingestor.py
  - backend/app/ingest/news_ingestor.py
  - backend/tests/conftest.py
  - backend/tests/provider_foundation/test_provider_normalization.py
autonomous: true
requirements:
  - DATA-03
  - DATA-04
user_setup:
  - Polygon.io API credentials available for local and VM configuration
  - Benzinga API credentials available for local and VM configuration
must_haves:
  truths:
    - Core code can consume market and news data through separate internal provider interfaces rather than vendor payloads directly.
    - Market and news records normalize into stable internal schemas with UTC timestamps and provider health metadata support.
    - The system has concrete market and news ingestion entry points that can produce near-real-time normalized updates during the active runtime window.
    - Provider-side failures have explicit typed handling rather than ad-hoc exception flow.
  artifacts:
    - backend/app/providers/base.py
    - backend/app/providers/models.py
    - backend/app/providers/polygon_adapter.py
    - backend/app/providers/benzinga_adapter.py
    - backend/app/ingest/market_ingestor.py
    - backend/app/ingest/news_ingestor.py
    - backend/tests/provider_foundation/test_provider_normalization.py
  key_links:
    - Provider adapters implement the shared contracts defined in backend/app/providers/base.py.
    - Normalization tests prove adapter outputs conform to backend/app/providers/models.py.
    - Ingestion entry points consume the provider interfaces rather than vendor payloads directly.
---

<objective>
Establish the provider contract and schema foundation for Phase 1.

Purpose: create vendor-agnostic market/news boundaries and the concrete provider update path before runtime, universe, and health logic are layered on top.
Output: backend provider contracts, normalized record models, initial Polygon/Benzinga adapter modules, ingestion entry points for provider updates, and provider-normalization tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-provider-foundation/01-CONTEXT.md
@.planning/phases/01-provider-foundation/01-RESEARCH.md
@.planning/phases/01-provider-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the backend foundation and shared provider contracts</name>
  <files>backend/pyproject.toml, backend/app/config.py, backend/app/providers/base.py, backend/app/providers/errors.py, backend/app/providers/models.py, backend/tests/conftest.py</files>
  <action>Create the minimal backend package structure needed for Phase 1 execution and testing. Define shared configuration primitives, separate provider interfaces for market data and news, explicit normalized record models, and typed provider error categories. Keep vendor field names and transport assumptions out of shared models. Use timezone-aware UTC fields in normalized records, and make sure the models are rich enough for later scanner metrics without trying to solve later-phase logic now.</action>
  <verify>cd backend && uv run pytest tests/provider_foundation/test_provider_normalization.py -q</verify>
  <done>Shared provider contracts exist, normalized record models are defined, typed provider errors are explicit, and the backend has the minimum test harness needed for Phase 1.</done>
</task>

<task type="auto">
  <name>Task 2: Add provider adapters and concrete normalized-update ingestion entry points</name>
  <files>backend/app/providers/polygon_adapter.py, backend/app/providers/benzinga_adapter.py, backend/app/ingest/market_ingestor.py, backend/app/ingest/news_ingestor.py, backend/tests/provider_foundation/test_provider_normalization.py</files>
  <action>Add the initial provider adapter modules for Polygon.io and Benzinga behind the shared interfaces, then create the concrete ingestion entry points that fetch provider updates and emit normalized internal records for later runtime-gated scanning. This plan must own the actual near-real-time provider update path, even though schedule gating and freshness evaluation belong to later plans. Back the adapters and ingestion entry points with canned fixture-based tests that prove market and news payloads normalize into the shared internal models. Avoid live network dependency in automated tests.</action>
  <verify>cd backend && uv run pytest tests/provider_foundation/test_provider_normalization.py -q</verify>
  <done>Polygon and Benzinga each have adapter modules plus ingestion entry points that produce the shared internal models, and the tests prove no vendor-specific payload shape leaks beyond the adapter boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/provider_foundation/test_provider_normalization.py -q
- [ ] Shared contracts exist for separate market-data and news providers
- [ ] Concrete ingestion entry points exist for normalized market and news updates
- [ ] Normalized models use UTC timestamps and remain vendor-agnostic
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Provider contracts, normalized schemas, and provider update paths are ready for downstream runtime and health logic
</success_criteria>

<output>
After completion, create `.planning/phases/01-provider-foundation/01-01-SUMMARY.md`
</output>

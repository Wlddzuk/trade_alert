---
phase: 01-provider-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - backend/app/ops/health_models.py
  - backend/app/ops/provider_health.py
  - backend/app/ops/degraded_state.py
  - backend/app/ops/system_events.py
  - backend/tests/provider_foundation/test_provider_health.py
autonomous: true
requirements:
  - DATA-03
  - DATA-04
user_setup: []
must_haves:
  truths:
    - Market-data and news freshness are tracked independently against configured thresholds.
    - The system can derive a single degraded/untrusted trust state when either provider is stale.
    - Freshness evaluation and degraded-state transitions honor the runtime-window contract rather than treating outside-window inactivity as stale-feed failure.
    - Recovery from stale to trusted state is explicit and testable.
  artifacts:
    - backend/app/ops/health_models.py
    - backend/app/ops/provider_health.py
    - backend/app/ops/degraded_state.py
    - backend/app/ops/system_events.py
    - backend/tests/provider_foundation/test_provider_health.py
  key_links:
    - Provider health logic consumes normalized provider timestamps rather than vendor payloads directly.
    - Degraded-state transitions emit system events that later operator surfaces can consume.
    - Provider health logic integrates with the session-window contract from Plan 02.
---

<objective>
Add provider freshness tracking and the degraded/untrusted trust-state foundation for Phase 1.

Purpose: make provider freshness a product-level trust gate that respects the configured runtime window.
Output: provider-health models, freshness evaluation logic, degraded/recovery state handling, and automated health-state tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-provider-foundation/01-CONTEXT.md
@.planning/phases/01-provider-foundation/01-RESEARCH.md
@.planning/phases/01-provider-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement per-provider freshness tracking and health snapshots</name>
  <files>backend/app/ops/health_models.py, backend/app/ops/provider_health.py, backend/tests/provider_foundation/test_provider_health.py</files>
  <action>Implement provider-health models and evaluation logic that track market-data and news freshness independently against configurable thresholds. Use a clear internal representation for observed time, last update time, freshness age, and health status. Integrate the runtime-window contract from Plan 02 so outside-window inactivity does not create false stale-feed failures. Keep the logic focused on provider trust rather than later alert-delivery or UI concerns.</action>
  <verify>cd backend && uv run pytest tests/provider_foundation/test_provider_health.py -q</verify>
  <done>Provider freshness can be evaluated deterministically for market-data and news feeds, and tests cover healthy vs stale cases.</done>
</task>

<task type="auto">
  <name>Task 2: Implement degraded-state and recovery event logic for actionable trust</name>
  <files>backend/app/ops/degraded_state.py, backend/app/ops/system_events.py, backend/tests/provider_foundation/test_provider_health.py</files>
  <action>Implement the foundation-level trust-state logic that marks the system degraded/untrusted when either provider becomes stale during the active runtime window and restores trust when both recover. Encode the Phase 1 rule that no partial actionable signals are allowed while only one provider is healthy. Do not implement Telegram or dashboard delivery in this phase; instead, emit explicit system events and state objects that later phases can surface to operators.</action>
  <verify>cd backend && uv run pytest tests/provider_foundation/test_provider_health.py -q</verify>
  <done>Degraded and recovery state transitions are explicit, actionable trust is gated correctly, and later phases have a clean event/state contract to surface operator notifications.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/provider_foundation/test_provider_health.py -q
- [ ] Freshness thresholds are evaluated independently for market and news providers
- [ ] Runtime-window rules prevent false degraded state outside the configured session
- [ ] Degraded state blocks actionable trust until both providers recover
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Provider freshness and degraded-state behavior are ready for later scanner and workflow phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-provider-foundation/01-03-SUMMARY.md`
</output>

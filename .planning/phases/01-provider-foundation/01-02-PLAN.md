---
phase: 01-provider-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - backend/app/runtime/session_window.py
  - backend/app/universe/models.py
  - backend/app/universe/filters.py
  - backend/app/universe/reference_data.py
  - backend/tests/provider_foundation/test_universe_and_schedule.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
user_setup: []
must_haves:
  truths:
    - The system can determine whether scanning is inside or outside the configured 04:00 ET to 16:30 ET runtime window.
    - The scan universe is restricted to eligible NASDAQ/NYSE common stocks and rejects excluded or ambiguous instruments.
    - Hard price and ADV filters are enforceable before later scanner logic runs.
  artifacts:
    - backend/app/runtime/session_window.py
    - backend/app/universe/filters.py
    - backend/app/universe/reference_data.py
    - backend/tests/provider_foundation/test_universe_and_schedule.py
  key_links:
    - Universe filters consume normalized instrument/reference metadata rather than scanner-specific logic.
    - Session-window logic is configuration-driven and testable against ET/UTC boundaries.
---

<objective>
Implement the runtime window and enforceable universe rules for Phase 1.

Purpose: ensure later scanner logic only runs at the right times and against the right instruments.
Output: session-window service, universe/reference-data eligibility logic, hard filter enforcement, and automated schedule/universe tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-provider-foundation/01-CONTEXT.md
@.planning/phases/01-provider-foundation/01-RESEARCH.md
@.planning/phases/01-provider-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement session-window logic using ET business rules and UTC-safe internals</name>
  <files>backend/app/runtime/session_window.py, backend/tests/provider_foundation/test_universe_and_schedule.py</files>
  <action>Create the runtime-window logic for the Phase 1 schedule. Use ET for business semantics and UTC-safe comparisons internally. Encode outside-window, premarket-active, regular-session-active, and post-cutoff behavior clearly enough that later scanner loops can depend on it without ambiguous edge handling. Keep out-of-hours scanning out of scope for v1.</action>
  <verify>cd backend && uv run pytest tests/provider_foundation/test_universe_and_schedule.py -q</verify>
  <done>Runtime-window behavior is explicit, configuration-driven, and covered by tests at the session boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Implement fail-closed universe eligibility and hard filter enforcement</name>
  <files>backend/app/universe/models.py, backend/app/universe/filters.py, backend/app/universe/reference_data.py, backend/tests/provider_foundation/test_universe_and_schedule.py</files>
  <action>Implement the initial scan-universe logic for US-listed common stocks on NASDAQ and NYSE only. Exclude OTC, ETFs, warrants, preferreds, rights, closed-end funds, and ADRs. Enforce the hard price and ADV filters before later scanner logic runs. Handle incomplete or ambiguous instrument metadata by failing closed rather than allowing borderline symbols through. Keep market cap and enhancement fields out of the hard-filter path.</action>
  <verify>cd backend && uv run pytest tests/provider_foundation/test_universe_and_schedule.py -q</verify>
  <done>Universe eligibility and hard filters are enforceable, ambiguous metadata fails closed, and automated tests cover both inclusion and exclusion cases.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/provider_foundation/test_universe_and_schedule.py -q
- [ ] Runtime schedule is limited to the configured 04:00 ET to 16:30 ET window
- [ ] Universe rules enforce NASDAQ/NYSE common-stock eligibility plus price and ADV filters
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Later scanner logic has a trustworthy runtime gate and a trustworthy initial universe
</success_criteria>

<output>
After completion, create `.planning/phases/01-provider-foundation/01-02-SUMMARY.md`
</output>

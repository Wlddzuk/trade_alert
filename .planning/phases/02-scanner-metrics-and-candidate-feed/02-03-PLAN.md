---
phase: 02-scanner-metrics-and-candidate-feed
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - backend/app/scanner/feed_store.py
  - backend/app/scanner/feed_service.py
  - backend/tests/scanner_feed/test_candidate_feed.py
autonomous: true
requirements:
  - DATA-05
  - SCAN-01
user_setup: []
must_haves:
  truths:
    - The live candidate feed keeps one current row per symbol, updates rows in place, and expires inactive rows after a configurable quiet period.
    - The live feed orders rows by freshest news first with `% move on day` as the tie-break until Phase 3 score/rank exists.
    - Candidate updates respect the Phase 1 trust gate and do not surface refreshed feed rows when provider trust is not actionable.
    - The same live feed can carry active rows from premarket into the regular session open without splitting the feed into separate ordered sections.
  artifacts:
    - backend/app/scanner/feed_store.py
    - backend/app/scanner/feed_service.py
    - backend/tests/scanner_feed/test_candidate_feed.py
  key_links:
    - Feed state consumes symbol-centric candidate rows rather than rebuilding row identity itself.
    - Feed updates consult the Phase 1 trust state before surfacing refreshed candidates.
---

<objective>
Build the live candidate-feed state and ordering behavior for Phase 2.

Purpose: turn candidate rows into the operator-facing live feed model chosen in CONTEXT.md, including in-place updates, inactivity expiry, unified feed ordering, and trust-aware update suppression.
Output: candidate-feed store/service, lifecycle and ordering logic, and automated feed-state tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-scanner-metrics-and-candidate-feed/02-CONTEXT.md
@.planning/phases/02-scanner-metrics-and-candidate-feed/02-RESEARCH.md
@.planning/phases/02-scanner-metrics-and-candidate-feed/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the symbol-keyed candidate feed store and row lifecycle rules</name>
  <files>backend/app/scanner/feed_store.py, backend/tests/scanner_feed/test_candidate_feed.py</files>
  <action>Implement the stateful candidate-feed store for Phase 2. The store should keep one live row per symbol, refresh that row in place, expire inactive rows after a configurable quiet period, and preserve feed continuity across the 09:30 ET transition when a row remains active. Keep this layer focused on lifecycle and state. It should not infer strategy-validity or replace the row semantics already fixed in Plan 02.</action>
  <verify>cd backend && uv run pytest tests/scanner_feed/test_candidate_feed.py -q</verify>
  <done>The feed store maintains one current row per symbol, handles inactivity expiry, and correctly preserves or removes rows according to the chosen Phase 2 lifecycle rules.</done>
</task>

<task type="auto">
  <name>Task 2: Implement feed ordering and trust-aware update behavior</name>
  <files>backend/app/scanner/feed_service.py, backend/tests/scanner_feed/test_candidate_feed.py</files>
  <action>Implement the live candidate-feed service that applies the provisional Phase 2 ordering and consumes the Phase 1 trust gate. The feed should sort by freshest news first and `% move on day` second, re-sort live as rows update, and remain unified across premarket and the regular open. It must suppress new or refreshed candidate surfacing whenever provider trust is not actionable. Do not add score/rank weighting or operator-surface delivery behavior in this plan.</action>
  <verify>cd backend && uv run pytest tests/scanner_feed/test_candidate_feed.py -q</verify>
  <done>The candidate feed can maintain the chosen live ordering, honor the Phase 1 trust gate, and automated tests prove ordering, suppression, and session-carryover behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/scanner_feed/test_candidate_feed.py -q
- [ ] The live feed keeps one current row per symbol and updates rows in place
- [ ] Inactive rows expire according to the configurable lifecycle rule
- [ ] Feed ordering uses freshest news first with `% move on day` as the tie-break
- [ ] Feed updates are suppressed when Phase 1 trust is not actionable
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 has a trustworthy live candidate feed ready for Phase 3 strategy-validity logic to consume
</success_criteria>

<output>
After completion, create `.planning/phases/02-scanner-metrics-and-candidate-feed/02-03-SUMMARY.md`
</output>

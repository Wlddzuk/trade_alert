---
phase: 02-scanner-metrics-and-candidate-feed
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - backend/app/scanner/models.py
  - backend/app/scanner/news_linking.py
  - backend/app/scanner/row_builder.py
  - backend/tests/scanner_feed/test_candidate_rows.py
autonomous: true
requirements:
  - DATA-05
  - SCAN-01
  - SCAN-04
user_setup: []
must_haves:
  truths:
    - The system can assemble one scanner row per symbol using latest-headline semantics rather than duplicating rows by news event.
    - Surfaced rows include the required Phase 2 market/news columns, including time since news and pullback % from high of day.
    - Row assembly stays compatible with later setup-valid and score/rank phases by avoiding strategy-validity decisions in this phase.
  artifacts:
    - backend/app/scanner/models.py
    - backend/app/scanner/news_linking.py
    - backend/app/scanner/row_builder.py
    - backend/tests/scanner_feed/test_candidate_rows.py
  key_links:
    - Candidate rows are assembled from normalized market metrics plus normalized news events.
    - Latest headline resolution drives the displayed headline, catalyst tag, and time-since-news fields for each symbol row.
---

<objective>
Build symbol-centric candidate row assembly for the Phase 2 scanner feed.

Purpose: convert normalized market metrics and news events into operator-usable scanner rows with the required Phase 2 fields and the row semantics chosen in CONTEXT.md.
Output: candidate-row models, latest-headline news-linking logic, row assembly logic, and automated row-field tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-scanner-metrics-and-candidate-feed/02-CONTEXT.md
@.planning/phases/02-scanner-metrics-and-candidate-feed/02-RESEARCH.md
@.planning/phases/02-scanner-metrics-and-candidate-feed/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement symbol-centric news linking and latest-headline resolution</name>
  <files>backend/app/scanner/models.py, backend/app/scanner/news_linking.py, backend/tests/scanner_feed/test_candidate_rows.py</files>
  <action>Implement the Phase 2 symbol-row semantics chosen during discuss-phase. Resolve multiple related news events into one current symbol view, where the latest related headline becomes the displayed headline, the displayed catalyst tag follows that latest headline, and time since news is measured from that latest update. Do not create per-headline scanner rows or drift into contradiction/invalidation logic from later phases; Phase 2 only needs deterministic latest-headline presentation.</action>
  <verify>cd backend && uv run pytest tests/scanner_feed/test_candidate_rows.py -q</verify>
  <done>News linking produces one current symbol view with the chosen latest-headline, latest-tag, and latest-age semantics, and tests cover duplicate/multi-headline scenarios.</done>
</task>

<task type="auto">
  <name>Task 2: Implement candidate row models and required Phase 2 row assembly</name>
  <files>backend/app/scanner/models.py, backend/app/scanner/row_builder.py, backend/tests/scanner_feed/test_candidate_rows.py</files>
  <action>Implement the row builder that emits operator-usable scanner rows from normalized market metrics and linked news context. Each row should expose the required Phase 2 columns, including symbol, headline/catalyst, time since news, price, volume, average daily volume, gap %, % change from prior close, daily RVOL, short-term RVOL, pullback % from high of day, and why surfaced. Keep row assembly strategy-light: no setup-valid, invalidation, or formal score/rank logic belongs here.</action>
  <verify>cd backend && uv run pytest tests/scanner_feed/test_candidate_rows.py -q</verify>
  <done>Candidate rows expose the required Phase 2 fields with the correct latest-headline semantics, and automated tests prove the emitted rows satisfy the Phase 2 row contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/scanner_feed/test_candidate_rows.py -q
- [ ] Candidate rows are symbol-centric and do not duplicate rows by headline
- [ ] Rows emit the required news, move, volume, and pullback fields for Phase 2
- [ ] No setup-valid, invalidation, or score/rank logic is embedded in row assembly
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 has operator-usable scanner rows that later feed-state logic can manage without redefining row semantics
</success_criteria>

<output>
After completion, create `.planning/phases/02-scanner-metrics-and-candidate-feed/02-02-SUMMARY.md`
</output>

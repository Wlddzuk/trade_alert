---
phase: 02-scanner-metrics-and-candidate-feed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/providers/base.py
  - backend/app/providers/models.py
  - backend/app/providers/polygon_adapter.py
  - backend/app/scanner/__init__.py
  - backend/app/scanner/history.py
  - backend/app/scanner/metrics.py
  - backend/tests/scanner_feed/test_metric_calculations.py
autonomous: true
requirements:
  - SCAN-02
  - SCAN-03
  - SCAN-04
user_setup: []
must_haves:
  truths:
    - The system can obtain normalized daily and 5-minute market-history inputs needed for configurable 20-day RVOL baselines without leaking raw provider payloads into scanner code.
    - Scanner metric logic can deterministically compute daily RVOL, short-term RVOL, gap %, % change from prior close, and pullback % from high of day from normalized inputs.
    - Phase 2 metric calculation stays strategy-neutral and does not encode setup-valid, invalidation, or score/rank logic.
  artifacts:
    - backend/app/providers/base.py
    - backend/app/providers/models.py
    - backend/app/providers/polygon_adapter.py
    - backend/app/scanner/history.py
    - backend/app/scanner/metrics.py
    - backend/tests/scanner_feed/test_metric_calculations.py
  key_links:
    - Historical market inputs remain behind the provider abstraction rather than being fetched ad hoc by scanner code.
    - Scanner metrics consume normalized market-history and snapshot records rather than raw Polygon.io payloads.
---

<objective>
Add the baseline market-history inputs and pure metric-calculation primitives for Phase 2.

Purpose: give the scanner a deterministic, vendor-agnostic way to compute the required market fields before any live row assembly or feed lifecycle logic is layered on top.
Output: normalized historical market-input support, baseline retrieval primitives, pure scanner metric calculators, and automated metric tests.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-scanner-metrics-and-candidate-feed/02-CONTEXT.md
@.planning/phases/02-scanner-metrics-and-candidate-feed/02-RESEARCH.md
@.planning/phases/02-scanner-metrics-and-candidate-feed/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend market-data inputs for normalized daily and 5-minute baseline history</name>
  <files>backend/app/providers/base.py, backend/app/providers/models.py, backend/app/providers/polygon_adapter.py, backend/app/scanner/history.py, backend/tests/scanner_feed/test_metric_calculations.py</files>
  <action>Extend the normalized market-data surface so Phase 2 can obtain historical daily bars and 5-minute intraday bars for the configured universe. Keep the provider boundary vendor-agnostic by adding normalized history records and a clean retrieval path rather than exposing raw provider payloads or scanner-specific request logic. Phase 2 only needs enough history support to power daily and same-time-of-day baseline calculations; do not expand into general charting or strategy-specific timeframe logic.</action>
  <verify>cd backend && uv run pytest tests/scanner_feed/test_metric_calculations.py -q</verify>
  <done>Normalized historical market inputs exist for daily and 5-minute baseline retrieval, and tests prove the baseline path does not leak vendor payload shapes into scanner code.</done>
</task>

<task type="auto">
  <name>Task 2: Implement pure scanner metric calculators for market move, RVOL, and pullback context</name>
  <files>backend/app/scanner/metrics.py, backend/tests/scanner_feed/test_metric_calculations.py</files>
  <action>Implement deterministic metric calculators over normalized market/news/history inputs for the Phase 2 row fields. This plan must compute gap %, % change from prior close, daily RVOL against the configurable 20-day baseline, short-term RVOL against the 20-day same-time-of-day 5-minute baseline, and pullback % from high of day. Treat pullback-from-HOD as a scanner field only in Phase 2; do not let this task drift into later impulse-leg retracement, setup-valid, or invalidation rules.</action>
  <verify>cd backend && uv run pytest tests/scanner_feed/test_metric_calculations.py -q</verify>
  <done>Metric calculators exist for the required Phase 2 fields, they operate on normalized inputs only, and automated tests cover the core formulas and baseline behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/scanner_feed/test_metric_calculations.py -q
- [ ] Historical daily and 5-minute baseline inputs are available behind normalized internal interfaces
- [ ] Daily RVOL, short-term RVOL, gap %, % change, and pullback-from-HOD calculations are deterministic and test-covered
- [ ] No setup-valid, invalidation, or score/rank logic appears in Phase 2 metric code
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 has the baseline and metric primitives needed for row assembly and live candidate-feed behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-scanner-metrics-and-candidate-feed/02-01-SUMMARY.md`
</output>

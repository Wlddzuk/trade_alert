---
phase: 07-served-dashboard-runtime-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/main.py
  - backend/app/api/__init__.py
  - backend/app/api/dashboard_routes.py
  - backend/app/api/dashboard_runtime.py
  - backend/app/api/dashboard_auth.py
  - backend/tests/dashboard/test_dashboard_serving.py
  - backend/tests/dashboard/test_dashboard_auth.py
autonomous: true
requirements:
  - FLOW-06
user_setup: []
must_haves:
  truths:
    - The served application exposes a real HTTP dashboard boundary with route dispatch that coexists with the Phase 6 Telegram webhook surface.
    - Dashboard access fails closed behind a lightweight password gate and reuses a browser-session cookie after successful authentication.
    - The application root redirects into the overview-first dashboard flow, and unknown dashboard URLs render a dashboard-specific not-found page.
    - Runtime dashboard responses are backed by a dedicated snapshot assembly seam rather than direct test-only renderer invocation.
  artifacts:
    - backend/app/main.py
    - backend/app/api/dashboard_routes.py
    - backend/app/api/dashboard_runtime.py
    - backend/app/api/dashboard_auth.py
    - backend/tests/dashboard/test_dashboard_serving.py
    - backend/tests/dashboard/test_dashboard_auth.py
  key_links:
    - Dashboard route dispatch extends `backend/app/main.py` without regressing the Telegram webhook behavior added in Phase 6.
    - Authentication stays local to the dashboard boundary and does not become a general app-wide auth framework.
    - Snapshot assembly reuses Phase 5 read-model seams so served pages stay aligned with existing dashboard composition behavior.
---

<objective>
Implement the served dashboard application boundary for Phase 7.

Purpose: turn the existing render-only dashboard into a reachable HTTP surface with route dispatch, fail-closed password access, and a runtime snapshot seam that can support served pages.
Output: dashboard route registration in the ASGI app, lightweight dashboard auth/session handling, runtime snapshot assembly entrypoints, and route-level tests for redirects, auth, and dashboard-specific not-found behavior.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-served-dashboard-runtime-surface/07-CONTEXT.md
@.planning/phases/07-served-dashboard-runtime-surface/07-RESEARCH.md
@.planning/phases/07-served-dashboard-runtime-surface/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the ASGI app to dispatch dashboard HTTP routes alongside Telegram webhook traffic</name>
  <files>backend/app/main.py, backend/app/api/__init__.py, backend/app/api/dashboard_routes.py, backend/tests/dashboard/test_dashboard_serving.py</files>
  <action>Broaden the served application boundary from Telegram-only JSON handling into a small multi-route HTTP surface. Add route dispatch for dashboard paths, HTML response handling, root redirect into the overview-first dashboard route, and a dashboard-specific not-found response for mistyped dashboard URLs. Preserve the existing Telegram webhook and callback behavior unchanged so Phase 7 adds a second served surface without destabilizing the live operator loop.</action>
  <verify>cd backend && uv run pytest tests/dashboard/test_dashboard_serving.py -k "route_dispatch or root_redirect or dashboard_not_found" -q</verify>
  <done>The ASGI app can serve dashboard routes and HTML responses, `/` redirects into the dashboard overview, unknown dashboard paths return a helpful dashboard-specific 404, and Telegram route behavior still passes through the same served boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Add a lightweight password gate, browser-session reuse, and a runtime dashboard snapshot seam</name>
  <files>backend/app/api/dashboard_routes.py, backend/app/api/dashboard_runtime.py, backend/app/api/dashboard_auth.py, backend/tests/dashboard/test_dashboard_auth.py, backend/tests/dashboard/test_dashboard_serving.py</files>
  <action>Implement the minimal dashboard-only authentication and runtime data seam required by the phase. Add a simple password prompt flow, fail-closed behavior when credentials are missing or wrong, and a browser-session mechanism so the operator authenticates once per session rather than on every page. In the same boundary, introduce a dedicated runtime snapshot provider that assembles the overview, incidents, review feed, paper P&amp;L inputs, and `last updated` metadata from existing Phase 5 read models so served routes stop depending on test-style direct object construction. Make the composition seam explicit in the app boundary: `create_app(...)` and `BuySignalApp` should receive dashboard runtime dependencies through constructor injection or a clearly-owned factory, rather than instantiating ad hoc globals inside route handlers.</action>
  <verify>cd backend && uv run pytest tests/dashboard/test_dashboard_auth.py tests/dashboard/test_dashboard_serving.py -k "session or fail_closed or auth" -q</verify>
  <done>Dashboard routes are protected by a simple fail-closed password/session gate, successful auth persists for the browser session, and a dedicated runtime snapshot seam exists for served dashboard responses over the existing read-model services with clear ownership at the `create_app(...)` and `BuySignalApp` composition boundary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/dashboard/test_dashboard_serving.py tests/dashboard/test_dashboard_auth.py tests/operator_workflow/test_telegram_webhook_serving.py -q
- [ ] The served app dispatches dashboard routes without regressing Telegram webhook handling
- [ ] Dashboard auth stays lightweight, dashboard-scoped, and fail-closed on missing or invalid credentials
- [ ] Root requests redirect into the overview-first dashboard route
- [ ] Dashboard-specific not-found behavior exists for mistyped dashboard URLs
- [ ] Served dashboard requests assemble data through a runtime snapshot seam built over existing read models
- [ ] `create_app(...)` / `BuySignalApp` explicitly owns how dashboard runtime dependencies are composed into route handling
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 7 has a served dashboard boundary with real route registration and protected runtime access for FLOW-06
</success_criteria>

<output>
After completion, create `.planning/phases/07-served-dashboard-runtime-surface/07-01-SUMMARY.md`
</output>

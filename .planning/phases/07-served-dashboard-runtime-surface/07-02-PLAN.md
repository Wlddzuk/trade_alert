---
phase: 07-served-dashboard-runtime-surface
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - backend/app/api/dashboard_models.py
  - backend/app/api/dashboard_routes.py
  - backend/app/api/dashboard_runtime.py
  - backend/app/dashboard/renderers.py
  - backend/tests/dashboard/test_dashboard_serving.py
  - backend/tests/dashboard/test_dashboard_runtime_state.py
  - backend/tests/dashboard/test_dashboard_review_and_logs.py
autonomous: true
requirements:
  - FLOW-06
user_setup: []
must_haves:
  truths:
    - Operators can reach overview, logs, trade-review, and paper-P&L sections through distinct served dashboard routes rather than direct renderer calls.
    - Served dashboard pages expose a visible last-updated timestamp, auto-refresh posture around 30 seconds, and stale warnings when runtime refresh fails.
    - Refresh failures preserve the last successful dashboard snapshot so runtime review remains available during incidents.
    - Route-level verification proves the runtime review flow works through the served app boundary while remaining explicitly read-only and secondary to Telegram.
  artifacts:
    - backend/app/api/dashboard_models.py
    - backend/app/api/dashboard_routes.py
    - backend/app/api/dashboard_runtime.py
    - backend/app/dashboard/renderers.py
    - backend/tests/dashboard/test_dashboard_serving.py
    - backend/tests/dashboard/test_dashboard_runtime_state.py
    - backend/tests/dashboard/test_dashboard_review_and_logs.py
  key_links:
    - Section routes must continue to render the Phase 5 overview, logs, trade review, and P&L information architecture rather than inventing a new surface.
    - Stale fallback semantics depend on the runtime snapshot seam from Plan 07-01 and should preserve the last successful snapshot when refresh fails.
    - Verification must exercise the served app boundary directly so FLOW-06 evidence is stronger than composition-only renderer tests.
---

<objective>
Implement the served dashboard review flow and runtime freshness behavior for Phase 7.

Purpose: complete the operator-facing HTTP dashboard by wiring section-specific runtime routes, visible freshness/stale cues, and served-boundary verification over the existing read-only dashboard surface.
Output: served overview/logs/review/P&amp;L routes, runtime freshness metadata with stale fallback behavior, and integration tests that prove dashboard review works through the served app boundary.
</objective>

<execution_context>
@./.codex/get-shit-done/workflows/execute-plan.md
@./.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-served-dashboard-runtime-surface/07-CONTEXT.md
@.planning/phases/07-served-dashboard-runtime-surface/07-RESEARCH.md
@.planning/phases/07-served-dashboard-runtime-surface/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose served section routes for overview, logs, trade review, and paper P&amp;L with visible freshness cues</name>
  <files>backend/app/api/dashboard_models.py, backend/app/api/dashboard_routes.py, backend/app/dashboard/renderers.py, backend/tests/dashboard/test_dashboard_serving.py, backend/tests/dashboard/test_dashboard_review_and_logs.py</files>
  <action>Complete the runtime route surface over the existing read-only dashboard composition. Add distinct served routes for the overview landing page, logs, trade review, and paper P&amp;L, keeping overview first and the rest separately reachable under the dashboard boundary. Extend the page models and renderer output to show a visible `last updated` timestamp, refresh posture around `30 seconds`, explicit read-only labeling, and mobile-safe section treatment without changing the Phase 5 information architecture into a new product shell or control plane.</action>
  <verify>cd backend && uv run pytest tests/dashboard/test_dashboard_serving.py -k "section_routes" tests/dashboard/test_dashboard_review_and_logs.py -q</verify>
  <done>Each dashboard section is reachable through its own served runtime route, overview remains the landing surface, page output includes visible freshness cues and read-only framing, and section rendering still matches the established Phase 5 review/logs/P&amp;L structure.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve the last successful snapshot on refresh failure and prove the full review flow through served-boundary tests</name>
  <files>backend/app/api/dashboard_runtime.py, backend/app/api/dashboard_routes.py, backend/app/dashboard/renderers.py, backend/tests/dashboard/test_dashboard_runtime_state.py, backend/tests/dashboard/test_dashboard_serving.py</files>
  <action>Implement the stale-state semantics locked in phase context. When runtime refresh or snapshot assembly fails, the server should keep serving the last successful snapshot, mark the page stale with a clear warning, and retain the last-known `last updated` context rather than blanking the dashboard during incidents. Add route-level verification for overview, logs, trade-review, and paper-P&amp;L access through the served app boundary, including authenticated access, stale fallback, and runtime data sourcing beyond composition-only renderer coverage.</action>
  <verify>cd backend && uv run pytest tests/dashboard/test_dashboard_runtime_state.py tests/dashboard/test_dashboard_serving.py -k "stale or section_routes" -q</verify>
  <done>The dashboard keeps the last successful snapshot visible when refresh fails, pages clearly indicate stale runtime state, and served-boundary tests prove the operator can review status, logs, and completed trades through real HTTP routes rather than direct renderer calls.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd backend && uv run pytest tests/dashboard/test_dashboard_serving.py tests/dashboard/test_dashboard_runtime_state.py tests/dashboard/test_dashboard_review_and_logs.py tests/operator_workflow/test_telegram_webhook_serving.py -q
- [ ] Overview, logs, trade review, and paper P&L each have their own served dashboard route
- [ ] Pages show a visible last-updated timestamp and a refresh posture around 30 seconds
- [ ] Refresh failures preserve the last successful snapshot and mark the page stale with a warning
- [ ] The dashboard remains explicitly read-only and secondary to Telegram across all served routes
- [ ] FLOW-06 evidence now includes served-boundary dashboard tests, not just direct renderer composition coverage
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 7 proves the runtime dashboard review flow works through the served app boundary for FLOW-06
</success_criteria>

<output>
After completion, create `.planning/phases/07-served-dashboard-runtime-surface/07-02-SUMMARY.md`
</output>
